# MLQT Backlog

The working list. Every open item has an id, and **an id is never reused** — they are cited from
code comments, test summaries, build scripts and CI workflows, so a new item takes the next number
above the highest ever issued, whatever has since been closed.

**B1–B271 have been issued.** B1–B167 were opened between 2026-09-03 and 2026-09-17 by the
seventeen end-of-branch reviews of the CI/CD toolchain and by phases 7a and 7b; B168–B203 by the
first end-to-end pass over the Photino release on 2026-09-17; B204 while settling B198's layout
question, B205 while fixing B192, B206 while trying to reproduce B198, B207–B209 while confirming
B172, B210–B211 from the resource reports in
Claytex and VeSyMA, B212 from WP8, and B213–B218 on 2026-09-18 from the decision that WP2 opens with
`analysis-viewer-fidelity.md` Part II, and B219–B223 on 2026-09-19 from the whole-solution
mutation audit, with B224 from what that audit did to this repository while running, B225–B226 from a second pass over the survivors asking which of them sit on code that writes to a user’s files, and B227–B229 as what that pass deliberately left for WP9, B230–B232 on 2026-09-19 from running WP2 step 1, B233 from the limit B214 shipped with, B234 from noticing that B213's strongest test runs nowhere automatically, B235 from measuring B185, B236 reported from ordinary use on 2026-09-19, B237 from a journey that could not be made reliable, B259–B260 on 2026-09-21 from using the external tool checks in WP5, and B261–B263 on 2026-09-21 from what B174's profiling left measured but unexplained and from the external tool interface changes in PR #9, B264 from doing B240 and finding the same assumption three paths wider, B265 from using the SVN history view on a real repository, B266 from what B265's own guards revealed about the suite filter, B267 on 2026-09-21 from WP7's mutation run refusing to start, B268 from tracing a Code Review defect back to a library index that claims classes it does not supply, B269-B270 on 2026-09-21 from one crash report: the diff viewer taking the application down, and what reloading after it did, B271 from the journey B237 exists to make possible, B272 from the tests B229 asked for finding a promise one of two paths had never kept, B273 on 2026-09-22 from the first test B227's survivor map pointed at, B274–B275 on 2026-09-22 from re-running the whole-solution campaign once B267 had made it mean what it says, and B276 from doing B274 and counting how many other places ask the same question by hand. Of B1–B167 all are closed
except the two carried forward below, and the table they lived in was retired with the phase design
notes on 2026-09-17 — git history has it if the reasoning behind one of those ids is ever needed.

New items start at **B272**. The watermark moves as items close, not only as they are opened: the id
guard checks that the ids *above* it run unbroken, so a closed row leaves a gap the moment it is
removed unless the watermark has advanced past it.

`MLQT.Cli.Tests/MarkdownTableTests.cs` holds this file's table structure and the uniqueness of the
ids. A blank line between two rows silently ends the table, and an unescaped `|` in a cell splits it
even inside backticks — both have happened, and neither is visible in review.

---

## Carried forward from phases 7a / 7b

Two items whose id is already in use elsewhere and whose work is not finished.

| # | Item | From | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B152 | **Twelve documentation screenshots are still photographs** | doc review 2026-09-11 | ⭐ | M | 41 of the 53 images are regenerated from the real UI by `DocumentationScreenshots`; the rest cannot be produced that way and are listed in CLAUDE.md and `skill-gui-testing.md`. Two of them need Dymola, six need an SVN server, one needs an SVN repository to render its settings section, one needs the merge dialog's ready-to-merge phase (which needs a VCS operation performed *in the application* to refresh working-copy status), and the rest show the window frame. **Not a defect** — recorded so nobody re-derives the list. Worth revisiting only if a fixture ever gains an SVN server. |
| B166 | **`check_library` once returned a different finding count on each run, and the variance has not reproduced** | 2026-09-17 | ⭐⭐ | M | The three causes found alongside it are fixed — the MCP call passed the wrong dictionary root, `modelsChecked` counted excluded libraries, and one `AnalyzeDependenciesAsync` call omitted the library roots so `modelica://` resolution depended on call order. The original 20,763 / 20,790 / 20,766 spread did **not** reproduce: three calls in one session and three with a full reload between them each returned 21,249. Two details weaken the original report — the reload path returns a **hardcoded** `affectedModelCount: 0`, so "no source change" was read off a literal; and the runs were scoped to `Modelica` alone inside a graph holding seven other libraries, which is a materially different shape. **Left open at low confidence.** If it moves again, capture the **per-rule breakdown of two adjacent runs** — a total says nothing about which rule moved. |

---

## Phase 1 — issues found testing the new build

Opened 2026-09-17 from a pass over the Photino release on both platforms. Mostly UI, not
exclusively. Grouped by area; the ids are in the order they were written down.

### Findings and the Code Review page

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B176 ✅ | **No way to search the code being reviewed** | Code Review | ⭐⭐ | S | Locating a parameter by name in a long class means scrolling. A search box over the rendered source, with match highlighting and next/previous, is what the page is missing to be usable on a real class. **Done**: a "Find in code" box beside the view buttons, a match counter, and up/down that step and wrap. The two halves search different things on purpose — the page finds the **lines**, over the text as the user reads it (tags stripped, entities decoded, so `<html>` finds the documentation string and `KEYWORD` finds nothing), and `CodeViewer` tints occurrences inside each token's own span, keeping its syntax colour. **The limit worth knowing**: a match spanning two tokens cannot be tinted, because the text is coloured token by token and `der(y)` is three spans — but the line is still found and stepped to, so it is reachable. No automated test covers the wiring between the two halves; that is B237. |
| B182 ✅ | **Line numbers for naming violations do not line up with the rendered code** | Code Review | ⭐⭐⭐ | M | A finding on a naming violation points at the wrong line in the viewer. The likely cause is that the finding's line is computed against the **stored** source while the viewer renders the **reformatted** source — the page reformats in order to colour it, so the two can differ by however many lines the formatter moved. Either the mapping has to go through the same representation the viewer shows, or the viewer has to stop reformatting. Establish which before fixing: the same mismatch would affect every rule whose line comes from a visitor, not only naming. **The viewer stopped reformatting (B215), so this closed as an identity rather than a fix**: a finding's line is counted against the class's own source, and that is now what is on screen. The guess in this row was right about the cause and right that it was not only naming — it was every rule. Measured before the change: only 3.7–5.8% of displayed lines were the file's line with the file's text on it. |
| B183 ✅ | **Findings do not scroll to their line, and arrive in no particular order** | Code Review | ⭐⭐⭐ | M | Two halves. Clicking a finding should scroll the viewer so the line it is about is **in view** — currently a finding on a parameter can select a class whose relevant line is off screen. And the list order is whatever the check produced; it should be stable and sorted alphabetically, so the same library reviewed twice presents the same list twice. **Both halves done with B215.** The order is by class, then by line within it, then by rule — which is how a reader works through a class, and is why it is not alphabetical by message. The scroll is a new `spellCheck.scrollLineIntoView` beside the word-scroll interop the spelling findings already used, and it only became possible because the viewer now shows the class rather than a copy 17–24% longer than it. A finding inside a collapsed nested class scrolls to that class's marker rather than nowhere, which is `SourceElision.ToDisplayLine` answering with the nearest honest line. |
| B186 ✅ | **The findings table runs off the bottom of the page** | Code Review | ⭐⭐⭐ | M | Part of the table is not reachable, particularly when each finding spans several lines. A splitter between the viewer and the findings list would let the user choose, but the row count shown has to vary with the space given to it as well, or resizing just moves which part is cut off. **Both halves done, and the row count does not vary — it is gone.** The table is virtualised and scrolled rather than paged, so every finding is reachable at any pane size and a real library's tens of thousands of them (26,557 for MSL) still cost only the rows on screen. The splitter is `MudExSplitPanel`, the control MainLayout already uses, turned the other way; the two fixed heights it replaces were `221px` for the table and `calc(100vh - 405px)` for the viewer, the second being the first plus 185 px for everything above it. `ResizablePanesJourney` drags it in a real browser and measures the pane, because that is the claim. |
| B187 ✅ | **The finding filter cannot filter by rule, and combines its terms with OR** | Code Review | ⭐⭐ | S | Two defects in one control: there is no way to narrow to a rule type, and entering more than one term widens the result instead of narrowing it. It should be **AND** — a partial model name *and* a keyword — which is what anyone typing two things into one box expects. **Both done, and the order mattered**: the class scope was smuggled through the search string (`_searchString + " " + NavState.ModelID`, stripped back out inside the filter), so swapping OR for AND first would have excluded every finding whose text did not also contain the class name — which is all of them. The scope and the rule are parameters now, and `CodeReview.Matches` is a pure function with fourteen tests over it. The rule list offers only the rules the current findings use, by catalogue title: offering all forty-odd makes the control something to search rather than something to pick from. |
| B178 ✅ | **The diff view uses its own colours instead of the syntax scheme** | Code Review | ⭐⭐ | S | Side-by-side diff markup does not follow the rendering colour scheme used when viewing a single file, so the same code is coloured two ways in two panes of the same page. The user's chosen preset should reach both. **Closed by the diff adopting `ModelicaTokenClassifier`** — it classifies each whole side once and indexes the result, rather than highlighting a line at a time, so the ~100 lines of keyword-list regex are gone. **The planning note's fix was not the one needed.** It said to share the *colour source* between two stylesheets, because the two highlighters "cannot be merged — a diff hunk is not parseable". Both halves turned out otherwise: the sides are whole documents, not hunks, so one classifier does serve both; and the preset already reached the diff, because the runtime stylesheet is global and every rule carries `!important`. What was actually wrong is that the regex could only produce four categories, so identifiers, types, names and functions were plain text in the diff and coloured in the viewer. Emitting all nine fixed it with no change to either stylesheet. |
| B185 ✅ | **A very large class can fail to render at all** | Code Review | ⭐⭐⭐ | M | An imported FMU — `Engines Examples I2 fmu` — was still not rendered after five minutes. Two candidate costs and they need separating before either is addressed: the syntax highlighting over a very large token stream, and the reformat the page performs in order to colour it. The likely answer is a size threshold above which the page shows plain source with no highlighting and no reformat, but measure which half costs the time first. Relates to B182, which is about the same reformat. **Measured, and it is neither half.** B215 removed the reformat; the highlighting costs 20–148 ms at every size tried. It is the **parse**, and not because of size — a run of comments inside an `equation` section makes it quadratic (**B235**), while 554 KB of annotated declarations parse in 367 ms. **The likely answer in this row was therefore wrong twice over**: there is no size to threshold on, and turning the highlighting off would have saved nothing. What ships instead is that the viewer stops *waiting*: above 64 KB the lexer paints the class at once and the tree's colouring replaces it when the parse lands, the difference being only whether an identifier is known to be a type or a call. The threshold is a judgement about when a stall would be noticed, not a prediction of one — said explicitly at the constant, because a number that looks like a prediction will be read as one. The cause is B235 and that is where the real fix is. |
| B213 ✅ | **A token classifier, so the viewer can colour the file instead of a rewrite of it** | Code Review | ⭐⭐⭐ | M | `ModelicaTokenClassifier` in `ModelicaParser`, beside `ModelicaRenderer`: walk the parse tree recording token index → category, then emit the **original** source verbatim with each token wrapped in the tag `CodeViewer` already consumes. Driven by character offsets, never by token text, so whatever the lexer skipped is copied through and the output is exact by construction. Three tiers — parse tree, lexer only, verbatim — so a class that does not parse is still coloured. Two property tests carry it: **round trip** (strip the tags, get the source back) over the fixture library, and **agreement** (same categories as `ModelicaRenderer` on already-formatted input). Measured at 99.98% / 99.77% agreement over 114,642 identifier tokens and byte-exact over 1.09M lines; see `analysis-viewer-fidelity.md` §4, §5, §9 and §14 S2. **Delivered** as `ModelicaParser/Visitors/ModelicaTokenClassifier.cs` with 31 tests. Round trip is asserted over a curated corpus always, and over **every `.mo` file in a real library** when `MLQT_FIDELITY_CORPUS` points at one — 8,367 files exact. Three corrections to the plan, all found by S0 and all in the walk rather than the emitter: the graphics level has **three** increment sites and the middle one is how a *named* argument's value gets one (that is what colours `DynamicSelect`); `der`/`initial`/`pure` are calls; and a dotted name is one tag to the renderer and one tag per IDENT here, so any comparison has to split it — but never inside a quoted identifier. **The tiering changed**: the lexer-only tier is chosen by the caller for a class too large to parse, not reached by failure, because the parser recovers rather than throwing. The one deliberate divergence from today's colours is B232. |
| B214 ✅ | **One range-elision mechanism, with a line map that inverts** | Code Review | ⭐⭐⭐ | M | Hiding annotations, hiding a package's class definitions, the MCP annotation strip and the package trimmer are four spellings of the same operation, and each is currently the renderer simply not visiting a subtree. In a verbatim view each becomes a dropped range of source lines, optionally replaced by a one-line marker. `SourceElision` — ordered, non-overlapping ranges plus `ToSourceLine`/`ToDisplayLine` — is that operation once, with invertibility as a property test. **Not deferrable**: every package opens with class definitions hidden, so this is how a package is displayed at all. `analysis-viewer-fidelity.md` §11b. **Delivered** as `ModelicaParser/Helpers/SourceElision.cs` (the mechanism) and `ModelicaParser/Visitors/ElisionFinder.cs` (the tree reading), with 30 tests; invertibility is a theory over nine range shapes and the package-collapsing gate is asserted directly. Overlapping ranges are refused rather than merged — there is no single answer for what the display shows, and a caller that produced them has a bug. **One decision that is not in the plan and is a real limit**: a construct is elided **as a unit or not at all**, so an annotation sharing its first or last line with code is left visible rather than leaving `Real x "d" annotation (Placement(` on screen. In MSL that hides the class-level graphics annotations, which are written on their own lines and are 41–44% of all lines, and keeps the inline `Placement` ones. Doing the mid-line case properly needs markup-aware splicing and is **B233**. |
| B215 ✅ | **The Code Review page shows the file, not the renderer's rewrite of it** | Code Review | ⭐⭐⭐ | M | Wire B213 and B214 into the page: the source rule (use the stored code while `SourceMatchesFile`, otherwise re-slice the file by `StartIndex`/`StopIndex` **with the decoder those offsets were recorded with**), the classifier in place of `ModelicaRenderer`, `ShowRawSource` deleted in favour of the lexer tier, and `PrependElementPrefix` inserting into verbatim text. **Closes B182, B183 and B185.** Measured: ~95% of displayed lines are currently not where the user's editor puts them, and the fidelity path is ~45% cheaper end to end. The page has no component tests today; they arrive with this. **Done, and it closed B182 and B183 with it — B185 is what is left** (the lexer-only tier is built and public but nothing chooses it yet, so the size threshold is still to come). `CodeReview.Show` is the whole of the new path: `ClassSource` for the text, `ModelicaTokenClassifier` for the colour, `SourceElision.Merge` for what is hidden, and the map kept beside the lines because one without the other cannot say which line of the class a displayed line is. **Three things went rather than being adapted**: `ShowRawSource` and the parser-error branch (the lexer tier colours what recovered, where the old path showed no colour at all), `LoadOriginalFileContents` orphaned with it, and `_modelicaCode`/`_lines`, which were written at three sites each and read nowhere. Ten tests on a page that had none; the one that matters asserts every displayed line equals the line the user wrote, over a deliberately loosely-formatted class the old path would have renormalised on its first declaration. |
| B216 ✅ | **`PackageCodeTrimmer` re-renders where it could excise** | Checking | ⭐⭐ | M | Trimming a package's inline standalone children runs them through `ModelicaRenderer`, so the checked text is a rewrite of the file and `SourceMatchesFile` goes false — which is why findings in those packages point at no line at all. Excising each child's `[StartIndex..StopIndex]` instead leaves a subsequence of the file, restores the line mapping, and improves the CLI and MCP reports as well as the viewer. **The riskiest change in the set**: the trimmer feeds the pipeline all three surfaces share, so the gate is the parity number (MSL = 34329) with no finding moving other than by the removed ranges. Two reassurances short of proof — the trimmer passes `FormattingOptions.None`, which reorders nothing, and no rule is sensitive to whitespace or line length. Decided by measurement before anything is built. **S1 decided it: in scope** — no finding is lost on either library, MSL gains 2 correct `OneOfEachSection` findings (excision leaves an empty section behind that re-rendering dropped, so it is baseline drift to declare), and the line map is exact over 30,599 lines. **Finish the file's mutation survivors with the rewrite.** Seven are left after B230 and each sits on a decision this item replaces or inherits: the selection rule at `:84` (both `&&` operands and the `"package"` literal), the three early returns at `:88`, `:96` and `:101` (nothing to exclude, the parse failed, a short class definition has no body), the parse-error guard at `:95`, and `excludeClassDefinitions: false` at `:106`. They have been in the WP8 audit report unclaimed since it ran; excision keeps the selection rule and the short-class case, and drops the rest with the render, so the honest end state is each one either killed by a test or gone with the code. **Done 2026-09-20.** The trimmer cuts the children's lines out instead of re-rendering the package, so what is left is the file's own text and `ModelNode.TrimElision` records which lines went - `ClassLocation.FileLine` takes the offset after putting them back. **Gate met on both libraries**: MSL 0 findings lost, exactly the 2 `OneOfEachSection` findings S1 predicted gained (and they are correct - `ReferenceAir.mo` really does have three public sections, which re-rendering merged), and **432 findings that used to be pinned to the class declaration now point at the line they are about**; Buildings 0 lost, 0 gained, 34 findings gained a line. Two things the item did not foresee. `SourceMatchesFile` was answering two questions - *do lines map* and *is this the whole class* - which the old trim made identical and this one separates, so `ClassSource` needed `StoredSourceIsWholeClass` or the diff view would have shown a trimmed package against itself. And the per-child file check had to be added: computing a child's range from a line number measured against a *different* file produced ranges that collided, and the whole trim was abandoned - B230's mistake one level down, caught by an existing test. **Survivors**: 3 of the 7 went with the render, 4 are killed by new tests, and the rest are recorded as judged-equivalent in the test file. One test written for them asserted nothing - a class named `package` cannot parse - and was removed rather than left. |
| B230 ✅ | **`PackageCodeTrimmer` re-renders packages that have nothing to trim** | Checking | ⭐⭐⭐ | S | A package whose standalone children are already in their own files has no inline child to remove — but the trimmer rendered it anyway, marked `SourceMatchesFile` false, and handed every later report a class whose lines were the renderer's rather than the file's. **377 of 688 trimmed packages in MSL (55%) and 1,172 of 1,235 in Buildings (95%) had nothing to remove**, and in Buildings 1,073 came out *longer* than the file. Fixed by one clause in the candidate filter: a child only counts if it shares the package's `ContainingFileId`. **Classes that lose their line mapping: MSL 688 → 311 (10.61% → 4.79%), Buildings 1,235 → 63 (16.44% → 0.84%)**, with the finding count unchanged on both (26,557 and 31,823) — a guard clause should move no finding, and it moves none. 174 MSL findings gain their true file line as a side effect; the remaining 654 are B216's. Sharing the file is the cheap half of the question — whether the child's `[StartIndex..StopIndex]` lies inside the package's is the exact one, and B216 asks it because it excises those ranges. `analysis-viewer-fidelity.md` §17.1. |
| B231 ✅ | **`ModelNode.StartIndex`/`StopIndex` are documented as something they are not** | Checking | ⭐⭐ | S | The field says "zero-based character offset ... in the underlying source file, read with `Encoding.Latin1` to match the parser" and that `StopIndex` is the last character, inclusive. Slicing a file that way reproduces **0 of 13,997 classes** across MSL and Buildings. They are offsets into the **line-ending-normalised** text `PreprocessCode` produces, and the slice that matches is `[Start..Stop+1]` — 99.49% / 98.38%, the residue being short class definitions, whose stored text carries a trailing `;` the slice does not. Every file in both libraries is CRLF, so a caller following the comment does not get a slightly wrong slice, it gets a different class. Correct the comment, and give the slice a helper so no caller has to know. `analysis-viewer-fidelity.md` §17.4. |
| B232 ✅ | **`ModelicaRenderer` colours array subscripts as function calls** | Code Review | ⭐ | S | `_isFunction` is a mutable field, not a scoped fact, and it shows in two ways. It **leaks into subscripts**: `VisitComponent_reference` visits the reference's `array_subscripts` while the flag is still set, so in `den2[i] := …` the subscript `i` is coloured as a call (377 tokens in MSL, 512 in Buildings). And a **nested call clears it** for the rest of the reference: in `m[integer(i),j] := …` the inner `integer(…)` resets it on the way out, so `j` loses the colouring the leak had given it (9 and 22). Found by the B213 agreement measurement, which is otherwise 99.998% — this is the entire residue. **The classifier should not reproduce either**, so closing this is mostly deciding that the visible change is wanted and saying so. **Done 2026-09-19.** `_isFunction` is saved and restored at each of the four sites that set it, and cleared around a reference's `array_subscripts`, which fixes both facets: a subscript is no longer a call, and a call nested in one no longer switches the colouring off for what follows. The classifier already declined to reproduce either, so the two now agree and `ASubscriptIsNotAFunctionCall_UnlikeTheRenderer` - which existed to record the divergence - is rewritten as `...AndTheRendererAgrees`. **Worth knowing before touching this again: the renderer's colouring is no longer what any user sees.** Every production call site passes `renderForCodeEditor: false` since B215; the tag-emitting mode survives only as the independent second implementation the classifier is measured against, which is the argument for fixing it rather than deleting it - a baseline with a known defect has to be explained every time it is used. Five tests, and the whole-library fidelity test re-run by hand (B234) since the renderer is that measurement's other side. |
| B235 ✅ | **A class with commented-out equations parses in quadratic time** | Parser | ⭐⭐⭐ | M | A run of comment lines inside an `equation` section makes `ModelicaParserHelper.ParseWithTokens` quadratic: **500 of them parse in 1.5 s, 1,000 in 4.4 s, 2,000 in 17 s, 4,000 in 69 s** — four times the work for twice the text, at only 323 KB. The same class with the comments removed parses in 86 ms, and 4,000 *annotated* declarations (554 KB, half as many lines again) parse in 367 ms, so it is neither size nor volume. **The input is legal Modelica and parses with zero errors**, so this is the parser's prediction rather than its error recovery — `equation_or_comment : (c_comment \| (equation ';'))` with `COMMENT` and `LINE_COMMENT` on the default channel is where to look, and `statement_or_comment` is the same shape. **This is not a viewer problem**: the check pipeline parses every class, so a library with commented-out equations costs the CLI and the MCP server the same minutes it costs the page. B185 only stopped the viewer *waiting* for it. Reproduce with `MLQT_LARGE_CLASS_COST=1` over `LargeClassCostTests`, which measures three shapes so the cause cannot be misattributed again — the first version of that measurement varied the annotations and the comments together and blamed the annotations. **✅ Done 2026-09-20, and it is not what the row named.** Reproduced first, varying one thing at a time, and the fourth shape gave it away: the **same comments followed by one real equation** parse in 22ms where the same comments at the end of the section take 16,813ms - **764×**. So it is not comments, and not `equation_or_comment`; it is a **trailing run** of comments in any `(c_comment \| X)*` loop, where the parser must scan the whole run at every comment to decide whether the loop has ended. `element_list` had it too, at 17,933ms for 2,000, which is why the fix covers all three loops rather than only the reported one. **The fix is one character in each**: `c_comment` → `c_comment+`, so a run is consumed in one iteration and the ambiguous decision is taken once rather than once per comment. Measured: 2,000 comments in an equation section **16,813ms → 79ms**, in an element list **17,933ms → 81ms**, both now linear; `plain` and `annotated` unchanged. **The gates, all of them**: 8,367 real files rendered before and after with **0 differing** - the write path is byte-identical - the classifier's whole-library round trip still exact, and MSL 5,250 / Buildings 3,285 findings unchanged. Four tests, asserting **growth rather than a threshold** because a millisecond count is a property of the machine and 4×-the-work-for-2×-the-text is a property of the parser. Reverting the grammar alone does not build - the call sites take an array now - so the coupling is its own guard. |
| B237 ✅ | **Nothing automated proves the code search is wired up** | Testing | ⭐ | S | B176's two halves are unit-tested — which lines match (`CodeReviewCodeSearchTests`) and what the markup looks like once tinted — but the wiring between them is not: the term travels from the field through `CodeReview` to `CodeViewer`, which re-renders only when it notices the parameter changed, and only a browser can say whether that happens. **A journey for it was written and then removed**, because it could not be made reliable in the shared host: searching needs a class selected, and the two ways to get one both fail there. Through the tree needs a *repository*, and registering one starts the analysis pipeline and leaves it registered — which failed the journey and then timed out `TabNavigationJourney` five minutes later. Clicking an injected finding avoids all that and **passes in isolation in 11 s**, but in a full run the code viewer never renders at all, for a reason in the shared host that was not found. The feature was verified by that isolated run and by the regenerated screenshot. Worth either a journey with a host of its own, or understanding why a class will not open when the other journeys have run first — the second is the more useful answer, because `ResizablePanesJourney` will hit it too the moment it needs a class. **Half of it is found and fixed, and the journey is still not shipped (2026-09-21).** The shared host had a cause nobody had named: every journey builds its own `LibraryFixture` under a fresh temp path and **every one of them is called `Lib`**, so they all produce the same class ids - and nothing ever took a library back out of the shared `ILibraryDataService`. `LibraryFixture.Dispose` deletes the directory and leaves the library registered, and `DirectedGraph.AddNode` keeps the copy that arrived first, so `Lib.Modified` resolved to the first journey's node whose file had been deleted with its fixture. That is why a class opens alone and never renders in a full run, and `ResizablePanesJourney` would have met it the moment it needed one. `TestHostFixture.ResetLibrariesAsync` now empties the graph at the **start** of each journey - at the start rather than the end, because a journey that fails half way through does not get to clean up. All 71 journeys pass with it. **It was not the whole cause.** The journey was written again (four tests: tinting appears, the count fills, clearing removes it, a term that matches nothing changes nothing) and it *still* passes alone and fails beside its own siblings - so it is not committed, because a test that depends on what ran before it is worse than no test. What is left is not about libraries: the circuit terminates with `InvalidOperationException: The current thread is not associated with the Dispatcher` out of `CodeReview.OnModelSelected`, which is **B271**. Fix that first; this closes behind it. **✅ Closed 2026-09-21, and the reason was found rather than worked around.** Two causes, both in the harness. The first: every journey's fixture library is called `Lib`, so they all produce the same class ids, and **nothing ever took a library back out of the shared service** - `LibraryFixture.Dispose` deletes the directory and leaves the library registered, and `AddNode` keeps the copy that arrived first, so a class resolved to the first journey's node whose file had been deleted with its fixture. `TestHostFixture.ResetLibrariesAsync` empties the graph at the **start** of each journey, because one that fails half way through does not get to clean up. The second, and the one the row could not name: **no journey ever closed a page**, and every open page is a live circuit while `AppState` is a singleton shared by all of them - so a page left open went on reacting to events raised by whatever page came after it, `CodeReview.OnModelSelected` ran on the raising circuit's dispatcher rather than its own, and the render call killed circuits no test was looking at, taking the current one's code viewer with them. `NewPageAsync` now closes the page it handed out last. **The desktop host has exactly one circuit, so none of it was reachable in the product** - which is why the fix is here and not in a component made to tolerate it. `CodeSearchJourney` is committed: four tests over the wiring B176 left untested - the tint appears, the count fills, clearing removes it, a term matching nothing changes nothing. **The suite is faster for it**: 75 tests in 59s where 71 took 2m05s, because the leaked circuits were costing time as well as reliability. |
| B238 ✅ | **A rule switched on while its prerequisite is off is lost on the next save** | Settings | ⭐⭐⭐ | S | Found while doing B181, and it predates it. The four rules with a prerequisite were serialized through a bool facade whose getter answers the *effective* question - will this rule run - which is false while the prerequisite is off. So a rule the user had switched on was written as `false`, and because the facade's setter removes the map entry when given false, reading the file back deleted the entry `RuleSeverities` ahead of it had just restored. A repository that turned `OneOfEachSection` off lost its ordering rules at the next save, silently, while the dialog went on showing them ticked - it binds to `IsRuleSwitchedOn` precisely so an inert rule still reads as configured, which is what hid it. The bool reconciliation was built to be order-independent and is, for `true`; the asymmetry is that disabling removes unconditionally, right for a caller saying "off" and wrong for a redundant `false` that only means "not currently in effect". **Fixed 2026-09-19**: the legacy JSON name carries the configured value and the effective facade is out of the JSON surface, so old settings files still load and no C# caller changes meaning. Six tests, driven off `RuleCatalog` so a fifth rule with a prerequisite is covered the day it is added. |
| B239 ✅ | **Nothing enforced the rule that every Modelica file read and write goes through `ModelicaFileEncoding`** | Testing | ⭐⭐ | S | Raised while reviewing B236: the funnel held because people remembered, not because anything failed if they did not. That is the shape this backlog keeps naming - a promise in CLAUDE.md with no test behind it - and B236 is what it looks like when it goes wrong, two write paths disagreeing for the whole life of the project. **Done 2026-09-20**: `ModelicaFileAccessPolicyTests` scans production source for raw file-text APIs and requires each to be in a ledger with a reason. It cannot tell what a `path` variable holds, so it does not guess - it puts the judgement in front of a human once and keeps it visible, the same trade `CodeBehindPolicyTests` makes. Counts are part of each entry, so a second call in an already-listed file is not covered by the first one's reason. Verified by mutation: pointing the incremental formatter at `File.WriteAllTextAsync` fails it. It found two sites a manual grep had missed and one real gap (B240), and the picker's Modelica entry point now uses the funnel. |
| B240 ✅ | **A conflicted file's `.mine`/`.rN` sidecars are read outside the encoding funnel** | Revision control | ⭐ | S | Found by B239's scan. `SvnRevisionControlSystem.GetConflictVersions` reads the two sidecar files SVN leaves beside a conflicted file with `File.ReadAllText`, which is a UTF-8 decode - so for a Windows-1252 library the conflict diff shows replacement characters where the accented characters and curly quotes are. **Read-only and display-only**: it feeds `ConflictDiffDialog` and nothing writes the result back, so the cost is a diff that looks wrong rather than a file that becomes wrong. **The fix is not simply to call the funnel.** `RevisionControl` has no project references at all - it is deliberately Modelica-agnostic - so reaching `ModelicaFileEncoding` means either a new dependency, which gives up that property, or passing a decoder in from the caller. Worth deciding rather than doing. **✅ Done 2026-09-21, and the decision went the other way from both options.** Neither a new project reference nor a decoder passed in: **`GetConflictVersions` returns the bytes it was given** and MLQT decodes them in `MLQT.Services/Helpers/VcsFileText`, through `ModelicaFileEncoding.DetectFromBytes`. A version control system stores bytes; what they mean is the caller's question, so the assembly that knows nothing about Modelica does not have to learn anything to stop getting this wrong. **Git had the same defect and the row did not say so** - `Blob.GetContentText()` decodes as UTF-8 exactly as `File.ReadAllText` does, and B239's scan could not see it because it was looking for raw file APIs. Both are fixed; fixing the reported one and leaving its sibling is the shape that produced B170's second and third reports. Five tests, one of which asserts the property that forced the shape - that `RevisionControl` still references none of our projects - because the tempting fix is the one that quietly breaks it. **The ledger entry in `ModelicaFileAccessPolicyTests` now says the reads are deliberate byte reads**; left alone it would have gone on describing the defect as current. **What this did not fix is B264**: the same UTF-8 assumption in `GetFileContentAtRevision` and in `SvnCli`'s stdout. |
| B264 ✅ | **Everything read out of VCS history is decoded as UTF-8, not just the conflict sidecars** | Revision control | ⭐⭐ | M | Found while doing B240, which fixed one path of several. `GitRevisionControlSystem.GetFileContentAtRevision` wraps the blob in `new StreamReader(stream)`, whose default is UTF-8, and `SvnCli` sets `StandardOutputEncoding = Encoding.UTF8`, so `svn cat` of a Windows-1252 file comes back with replacement characters before any MLQT code sees it. **It feeds the revision diff B202 just rebuilt** and the Code Review diff, so an accented library shows mojibake on both sides of a comparison of itself. **B240's answer applies and was deliberately not applied blind**: return the bytes and let `VcsFileText` decode them. It is a bigger change than the sidecars were - `GetFileContentAtRevision` hands a string to several callers, and the SVN half needs `SvnCli` to capture stdout as bytes instead of decoding it on the way out, which affects every command that reads output. Read-only, like B240: the cost is a diff that looks wrong, not a file that becomes wrong. **✅ Done 2026-09-21, the same way B240 went.** `GetFileContentAtRevision` is now `GetFileBytesAtRevision`: the VCS layer hands back what was stored and `VcsFileText` decodes it at the service boundary, through the same funnel a file on disk goes through. Git reads the blob's bytes instead of wrapping it in a default `StreamReader`; SVN gained `SvnCli.RunForBytes`, used by **`svn cat` and nothing else** - every other command writes text svn generated itself, which is UTF-8 by definition, so decoding that is right. **The ~50 tests that assert on text were not rewritten**: an extension method in the test project decodes for them, which is harmless where the fixtures are ASCII and is deliberately not offered on the production type, because a string-returning sibling there is what every caller would reach for. Seven tests, covering all three SVN routes (BASE, pegged revision, and by URL with the file gone from disk) - **mutation found that the first version only exercised the BASE route**, which is not the one the history diff takes. The Git entry in `ModelicaFileAccessPolicyTests`' ledger is gone, because the raw stream read it accounted for no longer exists. **What could not be demonstrated**: 45,929 `.mo` files across both local corpora - the SVN libraries and `C:\Projects\Modelica` - are all valid UTF-8, so no file on this machine reads differently before and after. The proof is at unit level, on files written as Windows-1252 by the tests themselves. |
| B265 ✅ | **Every SVN history diff failed to load the file** | Revision control | ⭐⭐⭐ | S | Reported from a real repository: viewing r39803 of `ModelicaLibrariesTrunk` and clicking any changed file gave *“Could not load file content for 'trunk/Modelica/Claytex/Electrical/CurrentControl.mo' at 39803”*. **Not caused by B202 - B202 changed the wording of the failure.** Two path spaces reach the VCS layer and nothing said which was which: `svn log` reports repository-root-relative paths (`trunk/Modelica/Foo.mo`), everything working from the checkout uses working-copy-relative ones. The dialog converted between them by stripping `trunk/` and testing whether the result was on disk - **against `repository.LocalPath`, while the lookup underneath used `repository.VcsRootPath`**. Those are different directories whenever a repository is registered at a library *inside* the checkout, which is how this one is registered (`...\ModelicaLibrariesTrunk\Modelica` against `...\ModelicaLibrariesTrunk`), so the strip never fired and `svn cat` ran against a path that could not exist. **The guess is gone**: when the working copy cannot answer, the SVN system asks the server - repository root first, then the working copy's own URL - which also fixes a revision **newer than the working copy** and a file deleted since, neither of which any amount of prefix-stripping could have reached. Verified against the reported repository and revision: the file now loads, and a revision from before it existed still returns nothing rather than the wrong content. **And the revision genuinely changed no lines** - r39803 is a merge whose change to that file is `svn:mergeinfo` only, which is why the commit lists dozens of untouched directories, so the empty-diff message now says that rather than leaving the user looking at a list that disagrees with it. Seven tests over the URL building, which is where the mistakes are (a doubled slash, a dropped branch, a library called `VeSyMA - Suspensions`); the server half stays hand-verified. |
| B266 ✅ | **281 SVN tests run nowhere, and one of them is failing** | Test fidelity | ⭐⭐ | M | Found while adding B265's guards, which the filter would have excluded. `run-all-tests.ps1` and CI run `RevisionControl.Tests` with `FullyQualifiedName!~Svn` because the SVN integration tests need a working copy and a server no runner has. It is a **substring** filter: the suite has 672 tests and 391 run, so **281 never execute anywhere**, and only two of the nine SVN test classes actually reference the repository-availability guard (`SvnIntegrationAdvancedTests`, 84 tests, and `SvnMergeCommitTests`, 6). The rest - `SvnCliTests`, `SvnOperationsTests`' filesystem-only conflict region, `SvnRevisionControlSystemTests`, `SvnToolLocatorTests`, `SvnChangedFilesSinceTests` - are excluded by their names alone. **And one excluded test is red**: `SvnIntegrationAdvancedTests.Commit_NestedDirectories_ReturnsSuccess` fails on the development machine, verified as pre-existing against an earlier commit in a worktree, which is B116's shape in a second suite - a red line nobody sees because the category is excused. The work is to classify by what a test *needs* rather than what it is called, which is a decision with real CI risk attached: a runner has no svn client, so a test that merely shells out fails there even though it needs no server. Do it by moving the server-dependent classes behind one explicit marker and running the rest, one class at a time, on a runner. **✅ Done 2026-09-21, by measurement rather than by reading the names.** With svn taken off PATH, exactly three classes fail - `SvnIntegrationTests`, `SvnIntegrationAdvancedTests`, `SvnMergeCommitTests` - and every other test passes, so the filter now names those three and nothing else. **157 more tests run in CI** (548 against 391, verified by running the suite in the runner's configuration: no svn on PATH, new filter, 548 passed). `run-all-tests.ps1` applies the filter **only when the machine has no svn**, so a developer's run covers all 673. The four copies of the filter are held together by `MLQT.Shared.Tests/SvnTestFilterTests.cs`, which also fails if the substring form comes back, and `WorkflowPlatformParityTests` now takes the string from it rather than writing it out again - four copies of a rule with nothing holding them together is what let this happen. **The hidden failure was a test, not the product**: `Commit_NestedDirectories_ReturnsSuccess` committed `level1/level2/level3/DeepFile.txt` to trunk on some earlier run, so every run since found it already versioned with identical content, staged nothing, and failed. The path is unique per run now. **It cost an hour to diagnose because the message was wrong**: a commit that succeeds and commits nothing reported *“SVN commit failed”*, so there was no error anywhere to find - it now says nothing was committed and why, with a test. |
| B241 ✅ | **`MLQT.Structure.SingleFilePackage` defaults to Warning** | Rules | ⭐⭐ | S | Raised by the user on 2026-09-20, from the question of what happens when another tool saves a new package as one file inside an existing repository: MLQT never restructures it on its own - the incremental path reformats it in place and leaves it single-file - so the only signal is this rule, and a rule nobody has heard of is a rule nobody turns on. **Done**: the first rule in MLQT that is on unless a repository turns it off. `RuleDefinition.EnabledByDefault` carries it, and the interesting part is that *absent* now has to mean two different things - so a default-on rule stores `"Off"` rather than removing its key, has no bool facade in the settings file (a bool cannot say "not configured", and writing one added a key to every file that was merely read and written back), and `SetRuleEnabled(true)` treats a stored Off as absent or switching it back on would find the key and leave it Off. **17 tests across four suites asserted that a fresh settings object has nothing enabled**, which is a fair measure of how load-bearing that was; `StyleCheckingSettings.NothingEnabled()` gives the idea a name. `mlqt check` gained a third state between "no rules" and "configured": *only the rules that are on by default*, because one rule's worth of findings reads like a clean bill of health. |
| B242 ✅ | **Split into files: a fix for a single-file package, from the finding** | Code Review | ⭐⭐ | M | Asked for on 2026-09-20 alongside B241: telling a user their package is stored wrongly and offering them **Format All Files** means reformatting thousands of files to correct one. `PackageSplitter` writes the package the finding names as a directory with a file per class and a matching `package.order`, deletes the file it came from, and touches nothing else - the parent's `package.order` already names it and still does. Offered on the finding's row and in the Finding Details dialog, and it asks before it writes. **What it acts on is what the rule reported**: both ask `SingleFilePackageAnalyzer.SplittableChildren`, so a package the rule did not report cannot be split and one it did moves exactly the classes the message named - the alternative being two copies of one decision, which is a shape this backlog has named four times. Two things came out of chasing its coverage: a package stored as `Sub/package.mo` with inline classes **is** reported by the rule and was being refused by the fix, which is offering a button that declines; and the case where the old file cannot be deleted now says the library holds the package twice rather than reporting success. 19 tests, against real files, including one that reloads the split library from scratch and compares the class list. |
| B243 ✅ | **Split into files destroyed a file when the package was nested in someone else's** | Code Review | ⭐⭐⭐ | S | Reported from real use on 2026-09-20, on MSL. Splitting `Modelica.Electrical.Spice3.Internal.Jfet` wrote Jfet into the Spice3 directory instead of Internal's, then **deleted the file it came from** - `Spice3.mo`, holding twenty-two other packages - so Internal disappeared from the library browser and Jfet was nowhere. **Two causes.** B177's rule reported every package nested inside a single-file package, not just the one that owns the file: 302 findings over 71 files on MSL, 23 of them for `Spice3.mo` alone, and only one of those 23 could actually be fixed - a nested package has no file of its own and cannot be given a directory until its parent has one. And B242's splitter took the directory of the file it found the package in, wrote there, and then deleted that file because what it had written was somewhere else. **Fixed on both sides**: the rule reports only the class that owns its file (65 findings, one per file, every one splittable), and the splitter refuses to delete a file that still holds a class it did not write - checked by removing the rule fix and watching the file survive. Eight tests on the exact shape, including one that asserts the *in-place* graph update matches a fresh load, because what the user saw was the library browser and not the disk. Nothing covered a nested package before, which is why a full suite passed over a fix that could delete a user's work. |
| B244 ✅ | **The rule missed a partly split package, and a saved settings file did not record it** | Rules | ⭐⭐ | S | Two things reported together on 2026-09-20 from opening MSL. **The rule said nothing about `Spice3/Internal/package.mo`**, which keeps eight classes inline beside thirteen siblings that already have their own files. B177 read the rule as *entirely in one file, or say nothing*, on the grounds that a part-split package was a different situation; a real library shows that half-way through is the ordinary shape of the drift, and silence is how a class stays put for years. It now reports every class that could have a file and does not - 74 findings on MSL where the whole-file reading gave 65. **And a settings file written before the rule existed never gained it**: pressing Apply wrote nothing, because absent already meant on. That hides a gate from everyone reading the repository and lets it move under them on an upgrade, so saving a repository's settings now records every default-on rule at the severity it is running with - only adding, never overwriting a decision. One consequence of the default worth knowing: a rule that runs in *every* check has to survive a broken node, or one unreadable class costs the whole library its analysis rather than costing itself. |
| B245 | **Two children whose names differ only in case are both kept inline, though only one would be a file** | Rules / formatting | ⭐ | S | Why MSL's `Spice3.Internal.JFET` is still in package.mo, which took a probe to work out. It has a sibling *package* called `Jfet`, and `ModelicaPackageSaver` refuses to store two children as separate entries when their names differ only in case - so the formatter keeps both inline, and `MLQT.Structure.SingleFilePackage` deliberately says nothing, because reporting them would offer a fix that moves nothing. **The rule is coarser than the filesystem is.** A package becomes a directory `Jfet` and a model becomes `JFET.mo`; those cannot collide. Comparing the entries that would actually be written - `Name.mo` against `Name/`, case-insensitively - would let all four of MSL's pairs split. It is a change to the **write** path, so it wants the parity run and a full-library save compared before and after, which is why it is written down rather than taken. Four pairs in MSL alone: MOS/Mos, MOS2/Mos2, DIODE/Diode, JFET/Jfet. |
| B246 | **`MLQT.Structure.UsesUndeclared` reports Modelica's own built-ins and annotation graphics** | Rules | ⭐⭐ | S | Reported 2026-09-20: FluidPower is told that library `Line` is referenced and not declared in its `uses(...)`, and `Line` appears nowhere in its package.mo - **every library reports this**, along with `Connections`, `ExternalObject` and `rooted`. Two distinct gaps, both confirmed by reading `UsesHygieneAnalyzer`, which has no notion of either. `Connections`, `ExternalObject` and `rooted` are **Modelica language built-ins**, not libraries, and no `uses(...)` can ever declare them. `Line` is a **graphical primitive inside an annotation** - `Line`, `Rectangle`, `Polygon`, `Text`, `Ellipse`, `Bitmap`, `Placement` and the rest - so the analyzer is treating annotation content as a reference to a library. The second is the bigger one: it is why *every* library reports it, and it means the rule is currently unusable. Fixing it wants a list of the language's own names (the specification has one) and for annotation subtrees to be excluded from reference collection. Check what the sibling rules do with the same names before assuming this is only the uses rule. |
| B247 ✅ | **The findings table does not say how many findings it is showing** | Code Review | ⭐ | S | With a search term, a rule filter and a scope all narrowing the list (B187), the number on screen is the only thing that says what the filters did - and there is none. A user cannot tell a filter that matched three findings from one that matched nothing but left the table looking plausible. Show the displayed count, and the total when they differ. Small, and it makes every other filter on that toolbar legible. **Four filters, not three**: the baseline toggle is one as well, and it was the only one the heading already accounted for. The heading now counts with `FilterFunc1`, the same predicate the table filters by, so the number above the rows cannot drift from the number of rows - which is the defect, stated precisely: the heading was computed from one set of findings while the table was filtered from another. Six wordings, in a pure `FindingsHeadingText` with its own tests, because the interesting part is which one applies and not the arithmetic. **The one judgement worth recording**: with the baseline toggle on, the denominator is the toggle's own count rather than the ledger - `2 of 7 changed findings`, not `2 of 40` - because the latter credits the search box with hiding the 33 the toggle hid, which is B247 again one level in. Three journey tests for the wiring, which no unit test can see, verified by putting the old count back and watching all three fail. **A blur is needed to drive that search box**: it is a plain `@bind-Value` with no `Immediate`, so filling it and reading the page straight away shows everything unchanged - true, and not the fault under test. `code-review.md` was stale on two counts beside this and is corrected: it described **two** filters where there are four, and said search terms are "matched independently" when B187 made them AND. |
| B248 ✅ | **The code search box sits too far from the match count and its arrows** | Code Review | ⭐ | S | Reported 2026-09-20. B176 put the find-in-code field at the left of the Code Review toolbar while the match count and the next/previous arrows are elsewhere along it, so the three parts of one control are read as three controls. Group them. **✅ Done 2026-09-20.** The three parts were adjacent in the markup all along - what separated them on screen was the count between them, which held `min-width: 84px` open whether or not it had anything to put in it. So the toolbar's resting state, nothing searched for, was a 220px box, 84px of nothing, and a pair of arrows that looked unrelated to either. They now sit in a stack of their own with no gap and the count sizes to its content, with margins applied by `:empty` in app.css rather than by a condition in the markup - one fewer thing to get wrong, and nothing to re-render when a count appears. Measured before and after: 84px to 8px. **One trap of its own**: the inner stack was given `AlignItems.Center`, which stops its children stretching - and the toolbar's buttons are sized by stretching to the row, so the two arrows came out 22px against 27px everywhere else. That is B249 reintroduced by the fix for B248, caught by B249's own test. |
| B249 ✅ | **"Go to class" is a different size from the rest of the toolbar, and its tooltip names an arrow that is not beside it** | Code Review | ⭐ | S | Reported 2026-09-20, from B197's navigation work. The button is larger than its neighbours - the others look to have grown and need shrinking back - and its tooltip says the back arrow returns, while the back and forward arrows live next to the current-class text box at the top of the page rather than in the toolbar. **Moving the arrows into the toolbar beside this button** puts one journey's controls together and makes the tooltip true. **✅ Done 2026-09-20, and the measurement contradicted the row.** "Go to class" was **32px tall where every other toolbar button was 44** - shorter, not larger - and the mismatch was specific to `Size.Medium` on a `MudMenu`, which renders 32 where a `MudIconButton` renders 44. The whole toolbar went to `Size.Small` on the user's decision, which is what CLAUDE.md asks for and what the rest of the application already uses; at Small the two agree. Asserted as **agreement rather than as a number**, because a test naming 27px would fail on a MudBlazor upgrade that changed nothing anyone could see. The arrows moved down beside the button, as asked. **That overturns B197 and costs something**: the history is one history and every tab moves the selection, so Dependencies, External Resources, Metrics and Settings now offer no way back. The user was told and chose it; `CurrentModelDisplay` and the toolbar both carry a note saying so and naming the other direction - bringing the used-classes menu up instead - if it bites. **One thing left**: `MudMenu` draws its own border rather than merging into the button group, so the three sit 33px apart at matching heights rather than seamlessly. It reads as one control and is not worth more time. |
| B250 ✅ | **The Code Review page scrolls as a whole, including the class name box** | Code Review | ⭐⭐ | S | Reported 2026-09-20: a vertical scrollbar appears for the entire page, so the current-class text box at the top scrolls away. Nothing on this page should scroll except the regions that declare they do - the code viewer and the findings table - which is the same shape as the double scrollbar fixed earlier in the findings pane, one level out. **Exactly that shape, and the row's guess at the cause was wrong**: not a panel being resized, but a height that never had room to be right. The viewer's splitter was `calc(100vh - 185px)` and measured in the browser its top sits at 173px, so the page came to `100vh - 12px`. **12px is not a margin, it is the absence of one.** The parse-error alert above it is 34px, so a class with a syntax error made the page taller than the window - and the first thing a page-level scrollbar carries out of view is the box saying which class you are looking at. The constant was a hand-count of five things nobody controls: the app bar, the pane margins, the current-class box, the tab header and the page's own toolbar. **Three pages had three different numbers for the same distance** - 185 here, 140 on Dependencies, 135 on External Resources, plus 210 for the resource splitter - which is the tell. So the chain from the tab pane down to a page is now a flex column and each page fills what is left, `min-height: 0` at every step, panels scrolling rather than clipping; the four constants are gone. The same fix as `.mlqt-findings-pane` one level out, and its note already said why: *a flex calculation rather than a subtraction, because the height is not a constant*. **Measured before and after, not read**: the tab host's stack was already 676px inside a 668px parent, 8px over, hidden only because `MudExSplitPanelItem` has `overflow: auto`. Three journey tests, verified by reverting the fix and inserting the real alert - unfixed the viewer stays at 535px and the page grows, fixed it gives the 34px up. Two DOM facts cost a round each and are worth knowing: `MudStack` renders as `d-flex flex-column` with **no** `mud-stack` class, and `MudExSplitPanel` renders a grid wrapper around the panel so styling one leaves the other sizing to its content. All 65 journeys pass and 28 screenshots were regenerated. |
| B251 ✅ | **Formatting rewrote every file's line endings, so a whole library showed as modified with nothing in the diff** | Rules / formatting | ⭐⭐⭐ | S | Reported 2026-09-20: **Format All Files** made every file in the library modified while `git diff` reported no differences at all, and the user's diagnosis - a line-ending change - was right. On a Windows checkout `core.autocrlf=true` hands you CRLF and stores LF, so a file MLQT rewrote as LF cleaned back to LF and the command line saw nothing, while **LibGit2Sharp**, which is what MLQT's own status reads, compared the bytes and called all 5,682 of them modified. **Structural, not accidental**: `ModelicaRenderer` joins its output with a line feed and nothing has ever converted it back, so every save path has always written LF whatever the file was. The answer belongs where the encoding's does and for the same reason - MLQT deciding how a user's file should look instead of writing it back the way it found it - so `ModelicaFileEncoding` now preserves the file's endings too, majority wins on a mixed file, and a new file keeps whatever its text came with rather than taking `Environment.NewLine`. That last was a second bug of the same kind: `WriteAllLines` used the platform's newline, so the same `package.order` came out CRLF on Windows and LF on Linux. **Measured**: formatting MSL twice now changes 1 file of 5,682, and that one is a file with no final newline gaining one (B236). Ten tests on the endings and one on the property a user meets - an already formatted CRLF library is byte-identical after formatting again. |
| B252 | **Components before classes does not order constants, parameters, variables and components** | Rules / formatting | ⭐⭐ | M | Asked for on 2026-09-20 after using B181: the expectation is **constants, parameters, variables, components, then classes**, and what the rule actually enforces is only the last boundary. It asks `element.class_definition()` against `element.component_clause()` and nothing else, so the four kinds of declaration can appear in any order among themselves - and `ModelicaRenderer` writes them as one undifferentiated `Element.Components` group, so the formatter could not produce the finer order either. **Two different problems behind one request.** `constant` and `parameter` come straight from `type_prefix` and are a syntactic question. **Telling a variable from a component is not**: `Real x` is a variable and `Resistor r` is a component, but so is `SI.Length x` a variable by every convention while its type is a class - so the declared type has to be followed through its alias chain to a simple type or not, which is exactly what `MissingUnits` already does and is the precedent to copy. **Decide the order before building it**: some teams write parameters before constants, and connectors conventionally group `input`/`output` together, so whether this is one fixed order or a configured one is the first question. Whatever it is, the formatter has to be able to produce it or the finding cannot be cleared - which makes this a **write-path** change carrying WP3's gate, a full-library save compared before and after. |
| B253 ✅ | **Exclude from formatting takes seconds to come back** | Code Review | ⭐⭐ | S | Reported 2026-09-20: about ten seconds on a 172-line file, where it used to be instant. B175 changed the button from adding a name to a settings list into writing an annotation into the source, so it now writes, re-parses and reloads a file - but **the log says that is not where the time goes**. For `Blocks.Continuous.LimIntegrator` the write, the reload and the monitor restart are all inside one millisecond at 11:21:06.0565, and the re-render does not begin until 11:21:12.83: **6.8 seconds with nothing logged at all**, and 11.8 seconds for the previous file. **One redundant call is removed on the strength of that**: the toggle ran `CheckModelVcsStatus()` synchronously on the UI thread, which asks the VCS for the whole working copy, while every other path on the page runs it through `Task.Run(...).ContinueWith(...)` for exactly that reason - and `OnModelSelected()`, called on the next line, already does. **Not yet proved to be the whole of it**: the working-copy answer is cached for five minutes and was warm 50 seconds earlier at 50ms, so something else may be in that gap - `CaptureScrollForReloadAsync`'s JS interop and `ModelContentChanged` are the other candidates. The toggle is now timed per step, so the next click says which. **Found on the third attempt, and only by measuring.** The first two explanations were wrong and are recorded because the pattern is the lesson: a redundant synchronous `CheckModelVcsStatus()` (removed, correctly, but not the cause) and then the scroll-position interop (bounded at 250ms, worth doing, and it turned out to take 117ms). **The eleven seconds was `BaselineStatusService`**. It follows file activity so its answer stays current, and MLQT's own writes are file activity: saving the annotation restarts the monitor and calls `NotifyFileActivity`, which raises the event **synchronously**. The refresh asks every repository for its working-copy status - a scan of thousands of files on MSL - and the throttle took its *leading* edge on the caller's thread while queueing everything inside the window. That is the whole of the reported pattern: the first click after an idle spell paid for the scan, the next two seconds later took the queued path and cost a sixth of a second. It now runs on its own thread like the trailing refresh, which nothing needed it not to - `OnChanged` is how a view learns the snapshot moved and fires either way. Two tests, verified by putting the synchronous call back and watching the notification take 2,008ms; the first version of them passed vacuously because a service with no libraries returns before it queries anything. The per-step timing said `write annotation: 11164ms` on the first click and 151ms and 161ms on the next two - and the file work inside that step is logged separately and finished 11.1 seconds before the step did. What is left after it is one thing: `CaptureScrollForReloadAsync`, a JS interop call that reads the code viewer's scroll offsets so the re-render can put the user back where they were. **Remembering the scroll position is a courtesy; making someone wait eleven seconds for it is not**, so the call is now bounded at 250ms and the edit completes without it when the browser does not answer, logging how long it took whenever it exceeds 50ms. Why the first call after an idle period is slow at all is not established - a WebView that has been idle for nine minutes is the obvious suspect, and every fast case followed another click within seconds - but it no longer costs the user anything, and the log now says when it happens. The synchronous `CheckModelVcsStatus()` removed earlier was redundant and is still worth having gone; it was not the cause. |
| B254 ✅ | **The variable on the left of an assignment is coloured as a function call** | Code Review | ⭐⭐ | S | Reported 2026-09-20 from reading `Modelica.Mechanics.MultiBody.Frames.Internal.maxWithoutEvent_dd`, whose whole algorithm section is `y_dd := if u1 > u2 then u1_dd else u2_dd;` — the target came out in the function colour instead of the variable one. The grammar is `statement : component_reference (':=' expression \| function_call_args)`, so the reference is the thing being **called** in one form and the thing being **assigned to** in the other, and both were being marked as a call. **The equation path beside it had always asked the question** (`equation.function_call_args() is not null`); the statement path answered `true` unconditionally. One line in each of the two visitors: `_isFunction` scoped around the reference in `ModelicaRenderer.VisitStatement`, and `IsCallTarget`'s `StatementContext` arm in `ModelicaTokenClassifier` - which had mirrored the renderer **on purpose**, its own comment naming this case as a known difference. **Fixed on both sides** for B232's reason: the renderer is no longer what a user sees, but it is the independent implementation the classifier's 99.998% agreement is measured against, and a baseline with a known defect in it has to be explained every time it is used. Six tests, three of them controls - a call statement with no `:=`, a `(a, b) := f(x)` multiple-output call, and an equation target - and verified by reverting both visitors and watching exactly the three non-control assertions fail. Nothing in 2,023 existing parser tests had pinned the old behaviour, and the whole-library round trip (B234's opt-in test, 8,367 files) still passes. |
| B234 ✅ | **The strongest test MLQT has does not run unless someone remembers a variable** | Testing | ⭐⭐ | S | `ModelicaTokenClassifierTests.RoundTripsOverAWholeLibrary` asserts the whole fidelity claim — strip the tags and the output is the source, over **8,367 files and 1.1M lines** of real Modelica. It is opt-in through `MLQT_FIDELITY_CORPUS` because a library that size is not in the repository, which means **neither CI nor `run-mutation.ps1` ever executes it**, and that is visible in the mutation report: the emit loop's offset arithmetic is where the surviving mutants are, because the only test that would kill them does not run. The committed corpus was widened (no trailing semicolon, trailing blank lines, CR-only endings) but that is a mitigation, not a fix. Three ways out and they are not equivalent: commit a corpus large enough to exercise the emit loop (how large is the question — the shapes that matter are multi-line tokens, skipped characters and text `PreprocessCode` has to tidy, not volume); make the CI job fetch a library; or accept it as a manual gate and put it in the release checklist so it is run deliberately rather than remembered. **Run it by hand before any release that touches the classifier** — the command is in CLAUDE.md. **✅ Done 2026-09-21, and the premise was wrong.** The survivors were not alive because the corpus test does not run - **they would have survived it running**. `RoundTripsOverAWholeLibrary` calls `Highlight(string)`, which ends in `catch { return Plain(source); }`, and `Plain` returns the source as untagged lines - which round-trips perfectly. So a fault that makes the emit loop *throw* is caught, falls back, and every round-trip assertion passes, over 8,367 files exactly as over eight. **A test that only strips the tags off cannot tell `Highlight` from `Plain`.** All three options above were aimed at the wrong thing. `TheEmitLoopIsFaithfulAndStillColours` goes through the three-argument `Highlight`, which has no catch, over the five shapes that reach the guards - no trailing semicolon, trailing blank lines, trailing spaces, and a character the lexer rejects both mid-file and at the end - and asserts the output is **tagged** as well as faithful, with a control test asserting that `Plain` round-trips and carries no tags so the second assertion cannot be dropped as redundant. Measured: **22 survivors to 19, 84.72% to 86.81%**. What remains in the emit loop is read and recorded at the test rather than scored: the `StringBuilder` capacity, and two pairs that are equivalent - the loop bound and the EOF `break`, each unreachable because of the other, and two comparisons whose mutated form appends a zero-length span. One logical mutant at the guard is not equivalent and no input was found for it. **The opt-in test is unchanged and now has a home**: `Design/release-checklist.md` exists (it did not), and running the corpus is item 2 with what it does *not* cover written beside it. |
| B233 ✅ | **Hide-annotations leaves the ones written on a line with code** | Code Review | ⭐ | M | `ElisionFinder` removes a construct **as a unit or not at all**, so an annotation whose first or last line carries real code stays visible — dropping whole lines from it would leave the user looking at `Real x "d" annotation (Placement(`, which is worse than showing it. That is the right default and it hides what matters (41–44% of lines sit wholly inside an annotation, and the class-level graphics ones are written on their own lines), but the inline `Placement` annotations on declarations are exactly the noise a user asked to hide and they remain. Doing it properly means splicing **markup**, not source — the elision is applied to highlighted lines, so cutting a line at a character offset can cut inside a tag. The shape is probably for the classifier to expose where each token's markup begins so a line can be rebuilt from whole tags. Worth measuring what fraction of annotations this actually leaves before deciding it is worth that. **Confirmed from real use 2026-09-20, and it is the common case rather than an edge one**: a `connect(...)` equation almost always carries its `annotation(Line(...))` on the same line, so a user who turns annotations off sees no change at all in the equation section - which is where most of the noise is. The measurement to make first is therefore not "what fraction of annotations" but "what fraction of *equation* lines". **Also asked at the same time: why is a hidden annotation replaced by a `// annotation` marker rather than simply removed?** The marker exists so the reader can see that something is hidden rather than silently reading a class that is missing parts - but it costs a line per annotation and the user found it noisier than the thing it hides. Worth settling deliberately: no marker, a marker only where a whole line went, or a gutter mark. **✅ Done 2026-09-20, measurement first as the row asked.** Over **8,367 files** of real Modelica: **31.6% of non-blank equation-section lines carry an annotation, and 62% of those were left visible** - so a user who turned annotations off saw no change at all in the section holding most of the noise. 65.9% of all annotations were hidden; it is now 100%, and 0 of the equation lines keep one. **The implementation is not the one the row predicted.** Splicing *markup* needs the classifier to expose where each token's markup begins, and cutting at a character offset can cut inside a tag - but removing an annotation from Modelica leaves Modelica, so `ElisionFinder.WithoutAnnotations` cuts the **text** and the caller simply colours what comes back. One more parse when annotations are hidden, and no second representation of where a token starts. Line numbers are preserved, so the existing map still points a finding at the right line: an annotation running across lines is joined onto its first and the rest come back as lines to drop. **The corpus found a defect no small test would have.** Those continuation lines were reported as elided but left in the returned text, so a caller that parsed it got the orphaned tail of an annotation whose head had gone - **3,199 of 8,367 files stopped parsing**, which in the viewer means silently dropping from parse-tree colouring to lexer-only. Emptying them fixes it; the check that found it is now part of the measurement and a unit test, verified by putting the bug back. **The marker question was settled: no marker anywhere.** It cost a line per annotation and was noisier than what it hid; the toolbar button being filled is the signal. A hidden nested class still leaves one and that is a different decision - it carries the class's *name*, so a package still reads as a list of what it contains. |
| B217 ✅ | **The class diff compares stored text against the file** | Code Review | ⭐⭐ | S | `LoadModelDiffAsync` feeds the diff `Definition.ModelicaCode` as the working copy and a slice of the **file** at HEAD. For a package whose inline children have been trimmed, or a class re-rendered by the formatter since it was read, those are not comparable documents — the trimmed children should be appearing as deletions. **Predicted, not observed**: confirm it against a package with standalone children and an uncommitted change before writing anything. B215's source rule fixes it as a side effect. **Confirmed, and the confirmation is the test**: a package the trimmer has touched holds text with its inline children removed, so every one of them showed as a deletion nobody made. `CodeReview.WorkingCopyText` now takes the working-copy side from `ClassSource` like the viewer does, so both sides of the diff come from the file. The control matters as much as the fix — an ordinary class, which is the large majority, still answers from its stored text without going near the filesystem. |

### Library browser and navigation

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B188 ✅ | **Repositories cannot be reordered within a project** | Library browser | ⭐⭐ | S | They appear in the order they were added, which is rarely the order of interest. The user wants the most relevant at the top. Needs a persisted order in the project settings and a way to change it. **Done, and it needed no sort key.** The list order already *is* the order — the browser renders `IRepositoryService.Repositories` straight through and `SaveRepositorySettingsAsync` writes the active project's entries from the same list, in order, which `LoadRepositorySettingsAsync` then walks sequentially. So the whole feature is `MoveRepository(id, delta)` plus up/down arrows in the Manage Repositories table. Two things worth knowing: the move is **saved where it is made**, because that tab has no Save button (the Settings page hides its action buttons for it), and a refused move saves nothing — a settings write rewrites every repository's `.mlqt/settings.json`, which is not a thing to do because an arrow was clicked at the end of the list. Out of range is a refusal rather than a clamp, so "moved" and "did nothing" stay distinguishable. |
| B189 ✅ | **Opening a model from a finding does not reveal it in the tree** | Library browser | ⭐⭐ | S | Clicking a finding opens the model in the viewer but leaves the browser wherever it was, so there is no context for what was just opened. Expand to and select the model, the way a "reveal in tree" action would. **Done**, and it reuses the tree's existing restore-expansion walk, which already knows to fetch children for a node marked expanded — a node expanded without them renders open and empty. Two things worth knowing: the chain of packages is walked through `ParentModelName` rather than by splitting the dotted name, because containment is what the tree nests by and a quoted identifier carries dots of its own (`ModelicaReference` has one); and a browser whose tree does not start at that chain's root returns without touching anything, or every repository's tree would expand for every class. A selection the tree itself made is not revealed — the user has just clicked it, and re-expanding underneath them is not help. |
| B191 ✅ | **Nothing in the tree marks a model as modified** | Library browser | ⭐⭐⭐ | M | Add a marker for models with uncommitted changes, and **distinguish the kind of change**: one marker for edits that affect simulation, another for purely graphical or documentation edits. Then a filter view showing only modified models, filterable by that distinction. The classification is the substantial part — it needs a comparison of the parsed old and new class, not a text diff. **Done, and the comparison is the deliverable.** `ModelicaParser/Comparison/` reduces each class in a file to two strings — a canonical token stream with comments, descriptions and display-only annotations removed, and its own source verbatim — and compares them against the committed version class by class. Equal meaning and equal text is `Unchanged`, equal meaning and different text is `Cosmetic`, different meaning is `AffectsSimulation`. **Both strings exclude the class's nested classes**, which carry their own answers, which is what stops a one-class edit marking every package above it. **Annotations are filtered, not ignored**: `SimulationAnnotations` is a list of the display-only ones, and everything else — including every vendor annotation MLQT has not heard of — is significant, because calling a graphical edit significant costs a second look and the reverse hides a real change from someone relying on the marker. Three things this had to get right that are not obvious: a class body's `annotation (...) ;` is a statement, so the semicolon goes with it or a class that gained nothing but an icon differs by a stray `;`; an external clause's semicolon is *not* that semicolon; and the elements of an annotation are a set, so a tool that rewrites them in another order has changed nothing. The tree marks **M** (orange), **G** (blue) or nothing, every chip now has a tooltip, the descendant dot is coloured by the strongest change under it, and a row of chips above the tree narrows to Changed / Affects simulation / Cosmetic only, each carrying its count. **The tree stays a tree** — it is pruned to the matching classes and the packages that contain them, built whole with the lazy loader withheld while a filter is on, and open exactly as far as the user had the full tree open. Three things were changed the same day after using it: the control was a dropdown over a flat list, `DescendantKinds` climbed the dotted id rather than `AncestorChain`, which is the mistake B189 recorded once already — a quoted Modelica identifier carries dots of its own — and the pruned tree first shipped fully expanded, which left the full tree spread out once the filter was cleared, the two sharing one expansion record. **When either version will not parse the answer is `Unknown`**, which reads exactly as MLQT did before this item. **Reported and fixed the same day**: none of this appears for a **reference-only** repository. MLQT never formats, checks, commits or writes to one, so there is nothing for a marker to be about — and it is not file-monitored either, so anything shown would only be refreshed by loading the project, which makes it a stale marker rather than a useful one. The whole pass is skipped, which also takes the working-copy query and the per-file committed-version reads off the startup path for every vendor checkout in a project. Same shape as B200. |
| B197 ✅ | **No way to peek at or navigate into a used class** | Library browser / Code Review | ⭐⭐ | M | Reviewing documentation, what looks like a variable name is highlighted and checking what it should be called means finding the base class by hand. Wanted: a quick peek, or navigation into the base class with a way back — ideally back through the last few classes visited, like an editor's navigation stack. **Navigation in and the way back, not the peek.** A menu on the Code Review toolbar lists the classes this one uses and opens one; back and forward arrows sit beside the class name, because every tab moves the selection and the history is one history — so it lives on `AppState` with the selection it tracks, not on a page. Going somewhere new after going back discards what was ahead, re-selecting the class already shown is not a visit (a reload re-raises the selection and would otherwise fill the history with one class), and fifty is as far back as it remembers. **What is not done is the peek**: hovering an identifier in the documentation and seeing its class without leaving. That needs a token to be resolved to a class, which the classifier's `TYPE` and `NAME` tags make possible but do not do. **And the menu is empty until dependency analysis has run**, because the edges are what it reads — it says so rather than looking like a class that uses nothing. |
| B173 ✅ | **The resource tree has no horizontal scrollbar and cannot be resized** | External resources | ⭐⭐ | S | A long path in the External Resources tree is simply cut off: the panel neither scrolls sideways nor resizes. Either would do; both is better, and a splitter here would be the same control B186 wants on the Code Review page. **Both done**: the fixed 4/8 grid became the same `MudExSplitPanel`, and the pane scrolls on both axes (`overflow: auto`, not `overflow-y`) so a path too long for whatever width the user chose can still be read. |
| B199 ✅ | **The dependency plot is unusable above a few hundred nodes** | Dependencies | ⭐⭐ | S | Over 1,000 nodes the Cytoscape graph is very slow to generate and the nodes are too small to read, so it costs a lot and shows nothing. Above a threshold, skip the plot and show the list alone — with a way to draw it anyway if the user insists. **✅ Done 2026-09-20, threshold measured rather than guessed.** Timed in a real browser, a layout settles in 377ms at 250 nodes, 435ms at 500, 539ms at 1,000 and **2,149ms at 2,000** for a force-directed layout - so generation only bites at the top end, and **that is not what makes the picture useless**. In a 400px panel five hundred nodes are a couple of pixels each. The limit is therefore about legibility: **500**, chosen with the numbers on the table, above which the impact list is shown alone with a **Draw it anyway** button. The override is per selection, so one decision to draw a large graph does not quietly disable the limit for the session. **The test is weaker than it should be and the gap is recorded rather than papered over**: the decision is a predicate with ten tests, but the one line of markup that calls it is uncovered. A rendered test was written and abandoned - the page runs its analysis twice during initialisation, asynchronously, so an assertion against the markup is a race. Worth returning to if the page is ever made testable; writing it down beats pretending the wiring is held. |

### Projects, repositories and startup

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B198 | **A newly added repository did not load its library until MLQT was restarted, and has not reproduced** | Repositories | ⭐⭐ | M | Reported once, against `ModelicaEditorTestsGit`; restarting the tool loaded it. **Not reproduced after several attempts** on a build carrying the fixes below. Two candidates were investigated and both are ruled out. The *layout* — a single library with `package.mo` at the top level — is disproven by `AddRepositoryLoadsItsLibraryTests`: such a library is discovered, loaded, recorded on the repository, announced to the tree, and visible to the `Libraries.Where(RepositoryId == id)` query MainLayout gates its analysis on, with a subdirectory layout as the control. **B204** — the discovered library being labelled with the folder name rather than its own — would have looked like this, but the library in that repository has the same name as its folder, so it was never visible there. **Left open at low confidence**, like B166. It is worth noting the report came before B192 was fixed, and that fix removed a path on which a *different* project's repositories were loaded at startup and left loaded — enough to confuse what belonged to what. If it recurs, B206 now makes it report itself: the two silent outcomes (nothing found, nothing loadable) each say so, so the next sighting can distinguish "no library discovered" from "discovered but not loaded" from "loaded but not shown", which the original report could not. |
| B190 ✅ | **The UI, and sometimes the whole desktop, freezes during loading** | Performance | ⭐⭐⭐ | M | Reported during the loading step: MLQT's window is unresponsive, and at some points other windows are slow to redraw too — moving the MLQT window is sluggish and everything can stall for a few seconds. **Confirm it still happens before investigating**; it predates several fixes since, and the log records phase durations that would say where the time goes. Whatever is blocking is on a thread it should not be on. **✅ Confirmed 2026-09-20 by the user running it, and the premise is retired: it does not freeze.** Neither MLQT nor the desktop. What is left is narrower and exactly diagnosable - *“during style checking when you click to drag the window it doesn't move with the mouse but it does jump to the new position when you release”*, plus stuttering rather than smooth dragging while loading. On Windows that is the signature of a **message pump that is not running**, and the desktop host runs its pump on the same thread Blazor renders on. **Measured**: a style check delivers findings a class at a time - 5,250 findings in **1,397 batches** for MSL on five rules, median batch **two** - and every batch raised `OnLogMessagesChanged`. Each raise cost that thread a **copy of the whole findings list** (the `LogMessages` property copies), a **sort of it** (`OrderedFindings` re-sorts on every render), a **full scan** for misspellings, and a **full re-render** of the page. So the work is O(batches × findings) on the one thread that must stay free. Fixed by coalescing: the first change is announced at once, anything inside 250ms collapses into one trailing announcement, and clearing stays immediate because the user is watching for it. 1,397 batches now produce fewer than 20 notifications. **And one of the costs was mine, from earlier the same day**: B247 added `all.Count(FilterFunc1)` to the findings heading - a full pass with a string split per element, on every render - which is now taken only when a filter is actually set. Four tests, the important one being that **the last change is always announced**: a throttle that swallows the trailing edge trades a stutter for a table that is quietly wrong. **Second round, and this time it really did freeze.** The user reported the window ignoring clicks to focus or move it for a long time during a Claytex check - a *blocked* pump, not a busy one - and the log gives the number: `Background style checking completed for (Claytex)` at **17:20:28**, `All background style checking completed` at **17:21:40**. **72 seconds after the workers had finished**, with nothing logged in between. That is the dispatcher draining a queue. `MainLayout.OnStyleFindingsFound` marshalled **every batch** through `InvokeAsync` - one queued work item per class with findings, 21,673 classes checked - onto the one thread that has to stay free. **It never needed to**: `CodeReviewService` locks around its list, so the store was always safe to call from a worker. The hop is gone; what does need the dispatcher - the render - is raised by the store, coalesced, and marshalled by whichever component is listening. `RecomputeMisspelledWords` and `RecountFindings` moved inside their `InvokeAsync` because they write component state and now run on a worker's thread. **One risk the change introduces and one flake that hinted at it**: a notification can now outrun a tab switch, so a component may be disposed between the event and its `InvokeAsync`; `TabNavigationJourney` failed once on exactly that shape and passed on every rerun, and both subscribers now swallow `ObjectDisposedException` for it. **Also visible in the same log**: with B174 in, Claytex's `MissingUnits` fell 2,076s → 334s and **`coverage` is now 69% of the check at 133ms/class** - the next thing worth measuring. |

### Analysis correctness

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B184 ✅ | **`--changed-from` re-checks everything it loaded** | CLI / performance | ⭐⭐⭐ | M | A changed-file check still runs every rule over every model, then filters. It should apply the rules only to the models in the modified files and compare those against the **baseline records for those models**. Everything still has to be *loaded* — base classes and reference resolution need it — but it need not be re-checked. The one analysis that genuinely has to run over everything is reference validation, and only when a model was deleted or renamed. This is the largest available win on check time in CI. **✅ Done 2026-09-20. Measured on the Modelica Standard Library**: a full check is 12.46s over 6,487 models, and `--changed-from HEAD~1` is **4.70s over 19** - **62% off** a typical CI run, with the remainder being the load, which cannot be skipped. **The sequencing moved as the plan said it must**: deciding which models a change touched needs the model→file map, which needs the load, so `CheckPipeline` takes a *callback* - given the map, return the models to check - rather than a parameter nobody could supply in time. `ClassLocation.ForGraph` moved ahead of the check to feed it; it only ever needed the graph. **The decision the plan demanded first, made and written into `cli.md` before any code moved**: a run asking for `--metrics`, `--min-coverage` or `--coverage-ratchet` **checks everything**, because coverage over the dozen models a commit touched is not that library's coverage and a ratchet recording it would move the baseline to a figure nothing can be compared against. It says so rather than narrowing quietly. **Both traps handled**: `StaleEntries` was already filtered to changed models on the output side and those models *were* checked, so it stays correct; `ModelsChecked` now counts what was checked rather than what was loaded, or a narrowed run would report the whole library's count and be indistinguishable from a full one. Five tests, verified by making the pipeline ignore the selection - two fail, including the one that matters most: an **unchanged model with the same finding must not be reported**. |


### Rules and formatting

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B175 ✅ | **Exclude-from-formatting writes a name into the settings file rather than an annotation** | Formatting | ⭐⭐ | S | The button records the class in `FormattingExcludedModels`, which is name-based and so does not survive a rename. `__MLQT(format=false)` is the rename-safe mechanism the documentation already recommends, both mechanisms are already honoured everywhere (B39, B65), and the writer already exists for suppression. The button should write the annotation. **Done 2026-09-19.** The button now writes `__MLQT(format=false)` into the class, through `MlqtSuppressionWriter`, and reads its own state through `FormattingExclusion.Excludes` rather than the name list alone - it was showing a class carrying the annotation as *not* excluded, so the button offered to exclude something that already was. **The row said the writer already existed, and half of it did.** Adding needed a bare directive rather than an entry appended to a quoted list, and *removing* one had never been needed by anything: the toggle has two directions, so `TryRemoveFormattingOptOutFromFile` is new. Removal widens as each container empties - the argument while `__MLQT` still holds something else, the whole `__MLQT(...)` when it does not, and the whole annotation and its `;` when that was all it held - because the alternative is leaving `annotation(__MLQT());` in a user's file. Re-including also clears the name list, or the button could not undo what earlier versions of itself had done. Reverting a modified file now happens *before* the annotation is written rather than after, which would have discarded it. 21 tests, including add/remove round trips that must return the original file byte for byte, and one across the two assemblies asserting that what the writer writes is what `FormattingExclusion` reads. |
| B177 ✅ | **No warning when a package is stored as one file instead of a directory** | Rules | ⭐⭐ | M | A package held entirely in a single `.mo` file rather than as a directory of standalone classes is a structural choice worth flagging in some repositories and not others, so it needs a rule id, a severity, a switch in the settings dialog and a row in the Findings list — not a hard-coded warning. Belongs with `PackageOrderAnalyzer`'s family. **Done 2026-09-19.** `MLQT.Structure.SingleFilePackage`, a graph analysis beside `PackageOrderAnalyzer`. The judgement is **could this be split**, not how big the file is: a child that cannot be stored standalone - `replaceable`, `redeclare`, `inner`, `outer` - has to live in its parent's file, so a package made only of those is correctly one file and is not reported, and neither is a package someone has already begun splitting, because "entirely in one file" is the claim. Off by default, like every rule, which is the point of it being a rule: single-file storage is a repository's choice. Nine tests, and the Structure category is catalog-driven so the settings dialog picked it up with no layout row. |
| B181 ✅ | **"Components before classes" can only be enforced by the formatter, never reported** | Rules | ⭐⭐ | S | The composition ordering rule is formatting-only: it is applied when the renderer rewrites a class and there is no way to be *told* about it. If the rule is switched on it should raise a finding like every other ordering rule. Note this row is the one in `settings-reference.md` with no rule id to bind its label to (B103), so giving it an id closes that gap as well. **Done 2026-09-19.** `MLQT.Style.ComponentsBeforeClasses`, with the visitor, the catalogue row, the layout row, the settings-reference id (which closes B103's gap - that row was the only one with no id to bind its label to) and the MCP toggle. Two things were not obvious from the row. **The section is the unit, not the class**: the renderer groups components before classes within each `public`/`protected` section and never moves an element across the boundary, so a class definition in one section followed by a component in the next is correctly ordered and comparing over the whole class would report the arrangement formatting produces. And its prerequisite is `ImportStatementsFirst`, not `OneOfEachSection` like its neighbours, because the renderer reads the option only inside the branch imports-first selects; the chain resolves transitively, so naming the nearer one states the real dependency. The settings switch now binds through `IsRuleSwitchedOn` like the four beside it, which it could not while it was a plain bool. |
| B236 ✅ | **Formatting strips the trailing blank line from every file, so a reformat rewrites the whole library** | Rules / formatting | ⭐⭐⭐ | S | Reported 2026-09-19 from ordinary use. **Format All Files** now writes every file without a final newline where it used to end with one, so a user who reformats sees *every file in the library* as modified against a revision formatted by an earlier build — a diff of thousands of files in which nothing has actually changed, and which buries any real change in it. **A regression, and it predates 2026.4.0**: the reported comparison is against a library formatted by an earlier version, and the release reproduces it. Two things worth knowing before hunting it. The write path ends `var code = string.Join("\n", visitor.Code)` (`ModelicaPackageSaver.cs:191` and `:357`) handed to `ModelicaFileEncoding.WriteAllText`, which writes exactly what it is given — so a trailing newline can only come from `visitor.Code` ending in an empty line, and the question is what used to put it there. And `ModelicaParserHelper.PreprocessCode` does `NormalizeLineEndings(...).TrimEnd()` before the parse, which is one way a trailing blank line stops existing early. **Bisect rather than reason**: this is a one-character output difference over a path several rewrites have crossed, and `git log` on those two files against a formatted fixture will find it faster than reading will. Whatever the cause, the fix wants a test that writes a file and asserts the last character, because nothing currently looks. **Fixed 2026-09-19, and the diagnosis in this row was wrong: it is not a regression.** `git log -S` settles it - the incremental formatter has appended a trailing newline since the initial public commit and the full library save has joined the rendered lines without one since the same commit. The two have never agreed, so which path last touched a file decided how it ended, and the user's own guess on the phase note - that the earlier reformat went a different route - was right. `package.order` was already terminated, because `WriteAllLines` mirrors `File.WriteAllLines`, so one save left the two kinds of file in a package ending differently. The rule now lives in `ModelicaFileEncoding.EnsureFinalNewline`, applied by every write and public so a caller comparing what is on disk against what it would write compares like with like - MCP's `format_class` needed exactly that, or it would have reported every formatted file as changed and rewritten it on each call. **Measured over MSL**: 5,753 files, none left without a final newline, and of the 2,480 files both paths rewrote the two now produce identical bytes for 2,471. The 9 that differ are all `package.mo` and are not a formatting disagreement - the full save extracts inline standalone children into their own files and the incremental path does not, which is the documented one-file-per-class restructure. Six tests, three of which state the route-independence property rather than the newline. |
| B195 ✅ | **`package.order` completeness, with an option to align with Dymola** | Rules | ⭐⭐ | M | Dymola emits, of its own accord on load: *"Warning: The package.order is incomplete, since the class Bad in file/directory .../Sub/Bad.mo is missing."* That is exactly the "a warning may be given" the specification anticipates, and it confirms the shape recommended for MLQT's own check. **It fires only for the `package.order` shape** — Dymola never descends into non-package folders, so it gives no signal at all for the stray-file case, and that half of MLQT's check has no upstream equivalent and earns its keep on its own. Worth an explicit "match Dymola" setting so a repository can choose to report only what Dymola would. **Done 2026-09-19, and the row was wrong about the interesting half.** `PackageOrderMatchesDymola` narrows `MLQT.Structure.PackageOrder` to what Dymola's loader would warn about. It is a modifier on one rule rather than a rule of its own - it decides which findings are reported, not whether anything is checked - so it is a switch under the analysis rules rather than a severity picker among them. **The 'stray-file case' this row rests on does not exist**: MLQT does not descend into a directory without a `package.mo` either (`LibraryDataService`, same reason Dymola does not), so there is no half of the check with no upstream equivalent on that side. Found by running the CLI over a fixture with a file in a plain folder and watching it not load. What the setting really drops is two things: **stale entries**, which Dymola says nothing about, and **a class in a file whose name does not match it** - MLQT reads every `.mo` in a package directory and takes each class's own name, so it loads `model Odd` out of `Widget.mo` and reports it unlisted, while Dymola resolves by file name and cannot load it from there at all. That second one is worth keeping in the default answer: a class the tool of record cannot find is a worse problem than the one this rule usually reports. Eight tests plus an end-to-end CLI check of both answers. |
| B174 ✅ | **Loading and style checking are getting slow** | Performance | ⭐⭐⭐ | L | Reported from ordinary use. B128 established the series and the per-phase breakdown — `CheckTimings` is in place and `mlqt check --timings` prints it — and concluded the growth to ~300 s was expected rather than a regression, with no next obvious win at the time. **Start from the log**, which records timestamped phase durations going back weeks, and from `--timings` on the current build; do not theorise first. B184 and B147 are the two named candidates. **Measured 2026-09-20, before touching anything, which is what this package's gate asks for - and the answer is not where the plan assumed.** From the application log, per class of real library: MSL 27.7ms total (parse 4.2ms, coverage 6.2ms), Claytex **314.4ms** (parse 11.3ms, coverage 161.4ms, MissingUnits 95.8ms). Checking MSL *inside* the Claytex session cost 24.7ms/class, unchanged - so it is **not the size of the graph**, which was the standing suspicion. Reproduced locally with the CLI on libraries in the repository's reach: with the same five rules on, `rule:MissingUnits` is **82% of the whole check on Buildings and 94% on MSL**, at 63.6 and 65.5 ms/class - within 3% of each other, so it is not a property of one library. **Parse is 6-17%**, which retires the assumption that B235 explains a useful share of this: it does not. **One change made, and two hypotheses tested and rejected first**, which is the part worth keeping. `CreateUnitLookup` memoised the *answer* - `unitCache` is keyed by the id of the class a type resolved to, so it holds nothing at all for a type that does not resolve, and resolving is the expensive half. Memoising the *question* `(class, type)` took MissingUnits from 425.1s to 295.4s on MSL and 477.8s to 310.6s on Buildings, with all 5,250 and 3,285 findings identical. Rejected: caching `TypeResolver.CollectAncestors` per class (**no measurable change at all**), and the theory that resolving a short name through an extends chain is dearer than resolving a qualified one (a synthetic library measured it the *other way round*, 99ms against 268ms). **~45ms/class remains unexplained** and wants a profiler rather than more reading - two guesses have already been wrong, which is the B253 lesson arriving again in the same package it was learned in. **Profiled 2026-09-20 with `dotnet-trace`, and the mechanism is now known.** Of real CPU (idle excluded), ~43% is ANTLR ATN prediction - the run is *parsing*, not resolving - and 76% of all non-idle samples have `MissingUnits` on the stack. The path between them, printed frame by frame: `MissingUnits.VisitComponent_declaration -> TypeResolver.ResolveWithInheritance -> CollectAncestors -> Walk -> EnsureParsed/ParseWithErrors`, **82% of the rule's time**. Collecting a class's extends chain reads each ancestor's interface, which parses it when nobody is holding its tree, and nothing memoised the chain - so a class with twenty unresolved type names re-parsed its whole ancestry twenty times. `TypeResolver.AncestorCache` now holds it for the run, scoped to the lookup rather than the graph because the graph is reloaded after a format or a VCS operation. **Measured end to end**: `MissingUnits` 425.1s -> 128.7s of thread-time on MSL and 477.8s -> 132.1s on Buildings, whole check 450.9s -> 159.5s and 579.4s -> 238.1s, with all 5,250 and 3,285 findings identical. **Parse is now the largest remaining phase on Buildings at 43%**, which is where B235 becomes worth taking. **The hour this cost is the lesson**: the ancestor cache was tried early, measured as "no change at all", and rejected - because the binary under test had not been rebuilt. The same change measured properly was a 56% cut. **Check the change is in the assembly before believing a negative result**; the profiling recipe is now in CLAUDE.md so the next person starts from a stack and not from a guess. **✅ Done 2026-09-20**, with WP4 - the reported symptom is fixed and the mechanism is known. The row was left without its tick while the backlog and the phase note disagreed about it, which is how an item that is finished goes on reading as outstanding work. **What is not finished is a different cause and now has its own id**: coverage measurement, measured at 69% of a check once the unit-resolution work was in, is B261. |
| B261 | **Coverage measurement is the largest single cost left in a check** | Performance | ⭐⭐ | M | With B174's unit-resolution work in, `coverage` is **69% of a check at 133 ms/class** - larger than any single rule and larger than parse. **The figure is recorded here without the library and build it was taken on, so re-measure with `mlqt check --timings` before touching anything**, which is what this phase's performance gate asks for in any case. **It is the same family as B174, and the first hypothesis is already visible in the code**: `UnitResolver.Resolve` takes an optional `TypeResolver.AncestorCache`, and `CoverageMeasurer` (`MetricsCalculator.cs:321` and `:336`) passes none - so measuring a class walks and re-parses its ancestry exactly the way `MissingUnits` did before that cache existed, which was worth 3x there. Its `_unitCache` is keyed by the **resolved class's id**, which is the *answer* shape B174 found insufficient; keying the *question* `(class, type)` was worth 30% on its own. **Neither is a reason to skip the profiler** - two guesses were wrong in B174 and the stack is what settled it. While there, ask whether `CoverageDimensions.ForClass` is doing per-class work that belongs per repository. |

### External tools

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B170 ✅ | **An OpenModelica check reports nothing at all** | External tools | ⭐⭐⭐ | S | Checking a class with OpenModelica produces no visible feedback: it opens no window, and there is no dialog reporting success or failure. The Dymola equivalent is only known to have run because Dymola's own window appears. Both need a result dialog of MLQT's own, reporting what the tool said — which also removes the dependence on a vendor window being visible. **✅ Done 2026-09-20.** `OnModelChecked` only recorded a finding when a check *failed*, and `OnCheckingComplete` closed the progress dialog and said nothing - so a clean check produced **no window, no dialog and no finding**, and the only evidence one had run was that the button had been pressed. Every result is now kept, not just the failures, and a dialog at the end names the tool and says how it went in one sentence: *“Dymola checked 27 classes with no problems reported”*, *“OpenModelica reported problems with 3 of 27 classes”*, *“Dymola check stopped after 5 classes”*. Failures quote the **tool's own message** rather than paraphrasing it - a dialog saying “check failed” only sends the user to the tool to find out why, which is what running it was meant to save - and they still go to the findings table, so closing the dialog loses nothing. Eight tests on the sentence, including the two wordings that would read as bugs: **“1 of 1 class”** and a cancelled run reported as a pass. **Reported wrong on first use 2026-09-21: a successful Dymola check said “Dymola checked nothing”.** The sentence was right and its input was empty - both services fired `OnModelChecked` **only for failures**, with a comment saying so *(“to reduce UI updates”)*, so a clean run produced no results to count. That saving is not one worth having now that the subscriber coalesces its own re-renders (B190). Every result is reported. **And `checkModel` returning true does not mean the tool had nothing to say** - the user's point: a model that is fine and one that is fine apart from six warnings both return true, and `getLastError()` is where the difference is. `ModelCheckResult` gains a `Log` separate from `ErrorMessage`, because putting warnings in a field called ErrorMessage makes every one of them look like a failure; the dialog shows it for a clean check too, when there is something to show. **One thing this had to get right to be safe**: Dymola's log accumulates, so it is cleared before the check and read after - otherwise a model that checked cleanly could be shown the error from one checked before it, which is a worse answer than no log at all. OpenModelica's `getErrorString` drains as it reads, so the equivalent there is to read and discard first. **Untestable without the tool**: neither service has a seam - `DymolaInterface` and `OpenModelicaInterface` are concrete - so nothing here guards “report every result”. That is why this was found by using it twice. **Reported again on first use 2026-09-21: OpenModelica refused to open the model at all.** Both services opened **the class's own file** - for `Modelica.Blocks.Continuous.Integrator`, `Integrator.mo`. A class stored in its own file is only called that because of the packages above it, and handed that file alone OpenModelica sees a class named `Integrator` with nothing to resolve its `within` against, and refuses. Dymola accepts it and discovers the enclosing package itself, which is the whole reason the same code worked for one tool and not the other - and exactly the shape where fixing only the broken one leaves the pair disagreeing about the same question. `LibraryRootFile.For(graph, model)` is that question asked once: the first segment of the class's name, resolved through the graph to the file that node came from, falling back to the class's own file for a one-file library or a class with no package above it. **Both** services use it. Seven tests, three of which fail against the previous behaviour - including one asserting only that the answer is *not* the class's own file, because that is what the fix is. |
| B171 ✅ | **Dymola checking fails on the second attempt if its window was closed** | External tools | ⭐⭐⭐ | M | The first check works; closing the Dymola window and checking again does not. The interface presumably keeps a connection or process handle that is no longer valid and does not detect it. It should notice a dead session and start a new one — and, with B170 in place, say so if it cannot. **✅ Done 2026-09-20.** `GetOrCreateAsync` returned the cached instance **without asking whether it was still there**. Closing Dymola's window ends its process and its JSON-RPC server, and nothing told the object holding the connection - `IsOfflineMode` is a flag set at construction, not a probe. **The probe already existed**: `DymolaInterface.PingAsync`, short-lived client, two-second timeout, written for scanning candidate ports. `IsAliveAsync` now asks the two questions cheapest first - a process we started that has exited needs no network call; anything else is pinged - and the factory drops and rebuilds rather than reconnecting, because the instance owns an `HttpClient` and a process handle that both describe the session that has gone. **Five tests, and the first four were worthless**: they all stopped at the `_isOffline` guard, so stubbing the ping to `return true` passed every one. The fifth stands up an `HttpListener`, connects to it, stops it, and asks again - the reported scenario as closely as it can be had without the tool - and it kills that mutant. **What is still unverified**: the factory actually rebuilding, because `GetOrCreateAsync` *starts* Dymola when it finds no live session, so the path cannot be exercised without launching it. Verified by hand is what the package asks for, and the 219-test Dymola suite passes against the live tool on this machine. |
| B259 ✅ | **Nothing happens for several seconds after the check button is pressed** | External tools | ⭐⭐ | S | Checking one class showed nothing at all until the check finished - no dialog, no progress, no sign the button had done anything. Worse for OpenModelica, which has no window of its own to appear. **✅ Done 2026-09-21.** The progress dialog existed but was opened **only for a package**, and even then it opened onto nothing: a check does not begin with the first class - the tool has to be started and the library opened, which is most of the wait on a real library - and both counts are zero throughout that, so the dialog read *0 checked out of 0*. Three changes, each small: the dialog opens on the click whatever is being checked; `ModelCheckProgress` carries a `Status` for what is happening before there is anything to count, which both services report as they start the tool and as they open the library (*“Opening package.mo in OpenModelica…”* - worth naming, since which file that is had just changed in B170); and the title counts classes only when there is more than one, because *0 of 1 classes checked* is a progress bar with nothing to say. Four service tests assert the **order** rather than the content - the first event reaches the UI while the factory is still blocked, which is the reported complaint stated as a test - and all four fail if the status is not reported. |
| B260 ✅ | **A headless OpenModelica process is left running after MLQT closes** | External tools | ⭐⭐ | S | `omc` has no window and no owner, so one started for a model check outlives MLQT and sits there until somebody finds it in a task manager and wonders what it is. **✅ Done 2026-09-21.** The factory is now `IDisposable` and the host ends the session after `app.Run()` returns. **Dymola is deliberately not treated this way** - its window is visible, the user may have carried on working in the session MLQT started, and closing it from underneath them would lose that work; what makes the omc process worth ending is exactly what makes it invisible, and a test states that as a decision rather than leaving it to look like an oversight. Found on the way: `OpenModelicaInterface.Dispose` set `_isDisposed` **before** calling `ExitAsync`, which asks `IsConnected` - so the graceful `quit()` had never once been sent and omc was always killed, leaving its temporary directory behind. Four guards, of which two are the whole of what is testable without OpenModelica installed; deleting the host's one-line call first left them green, because `ShutDownOpenModelica(` also matches the method's own declaration below it. |
| B262 | **PR #9 gives `DymolaInterface` a settable command timeout, and nothing in MLQT uses it** | External tools | ⭐⭐ | S | PR #9 (`dymola-command-timeout`, in review) removes the five-minute `HttpClient.Timeout` - which could never be raised afterwards, because the constructor's offline probe sends a request and `HttpClient.Timeout` is fixed once one has gone out - and replaces it with a per-request `CancellationTokenSource` built from a new `CommandTimeout`: read afresh per command, so it can change at any point in the interface's life, defaulting to the same five minutes and accepting `Timeout.InfiniteTimeSpan`. It also exposes `ProcessId`. **Decide how MLQT should use both once it merges.** The checking services are the caller that knows a check may be long, and `ProcessId` is a cheaper liveness question than B171's ping for a session MLQT started itself. Take it with B263, which is the user-facing half of the same question. |
| B263 | **A user cannot give an external tool longer for a large model** | External tools | ⭐⭐ | M | A complex model can take longer to check than the wrapper is prepared to wait, and nothing in the UI lets the user say so. **The two tools are in different states and neither is right.** PR #9 gives Dymola a `CommandTimeout` with no setting behind it (B262). `OpenModelicaSettings` has carried `CommandTimeoutMs` (60s) and `StartupTimeoutMs` (5s) since it was written and **neither is read by anything** - `SendCommandAsync` does a bare `ReceiveFrameString()` with no timeout at all, so a long omc check blocks indefinitely and `StartAsync` waits a hardcoded 2,000ms instead. Needs: omc honouring its own settings (`TryReceiveFrameString`), one timeout per tool in the External Tools settings tab, and the checking services passing it down. **A settings field nothing reads is worse than no field** - it is a knob that does not work, which is what both of these are today, and B259's progress dialog is where a long wait now has to stay honest. |

### MCP server and McpTester

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B179 | **MCP loads an encrypted library but returns almost nothing about it** | MCP | ⭐⭐⭐ | M | An agent can load an encrypted library, but the tools do not return the public interface or the documentation — both of which **are** recovered from the vendor's help HTML and are sitting on the synthesized stub: description, base classes, parameters, connectors, inputs and outputs. `get_class_info` and its neighbours should return them for a stub, clearly marked as recovered from documentation rather than read from source, and still declaring the class not editable (B85). |
| B196 | **The diagram tools give an agent no way to see what it drew** | MCP | ⭐⭐ | M | The diagram tools return a description, so the agent is laying out a picture it cannot look at. Rendering the SVG to **PNG** and returning it would let the agent judge its own layout — and the SVG is already produced, so the question is which conversion to take on (and what it costs in dependencies). Alongside that, the guidance the tools give needs to say how connector positions should be chosen to match MSL and the usual conventions, and it is worth asking what else would make an automatically generated diagram look right. |
| B218 | **`get_class_source` hands an agent a rewrite when it strips annotations** | MCP | ⭐⭐ | S | With `includeAnnotations: false` the tool re-renders the class through `ModelicaRenderer`, so the text an agent reads is not the file and its line numbers do not match the findings the same server reports. B214's elision strips the annotations without touching anything else, and then they do. Small, independent of the viewer work, and it can be taken in any phase after B214. |

### Revision control

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B193 ✅ | **Switch Branch cannot reach a Git tag** | Git | ⭐⭐ | M | Switching to a tagged version of `ExternData` is not offered, although TortoiseGit does it on the same working copy. The branch selector presumably enumerates branches only; checking out a tag produces a detached HEAD, which the surrounding UI also has to be able to describe rather than showing an empty branch name. **✅ Done 2026-09-21.** `GetBranches` lists tags beside branches for Git, flagged `IsTag`; the selector groups them under a `tags` folder and chips them as tags; `SwitchBranch` falls back to a tag and checks out its commit. **Annotated tags were the easy thing to get wrong**: their ref points at a tag object rather than a commit, so anything reading `Target` directly loses exactly the tags a release process creates - `PeeledTarget` is the one that handles both, and a theory covers each kind. **The second half is the half that would have been left out**: checking out a tag detaches HEAD, so `GetCurrentBranch` correctly returns null and the browser showed a blank. `GetDetachedHeadLabel` answers where HEAD is - the tag if it is on one, otherwise a short commit id - and the header now reads *“Detached HEAD at v2.0.0”*. Offering a switch into a state the application cannot describe would have been worse than not offering it. The dialog says what will happen **before** the switch rather than after. SVN needed nothing: its tags are directories and have always arrived as `tags/*`, and a test says so, so the new grouping cannot quietly take that path over. Eighteen tests across the VCS layer and the two components, each verified by mutation. **Reported on first use 2026-09-21: the dialog jumped to the width of the screen when a tag was clicked.** The content carried a `min-width` and no width of its own, MudDialog sizes to its content, and `MudDialogProvider`'s default cap is `MaxWidth.Large` - so the paragraph explaining detached HEAD, which appears on the click, stretched the dialog underneath the pointer that caused it. The content now has a width (`460px`, `max-width: 100%` for a narrow window) and the dialog is opened `MaxWidth.Small`. Three tests: a headless render cannot measure a layout, but it can hold that the declaration is there and that clicking a tag still produces the alert - between them the reason it used to resize. |
| B202 ✅ | **The VCS History file diff compares against the working copy** | Git / SVN | ⭐⭐ | S | Clicking a file in a commit's changed-files popover opens a diff of that revision against the **working copy**. What a user reviewing history wants is what that commit *changed* — the selected commit against its predecessor. B155 corrected the documentation of the current behaviour after finding it reported confusing numbers; this changes the behaviour to the one the dialog is for. **✅ Done 2026-09-21.** The dialog now shows the commit's predecessor on the left and the commit on the right. **The predecessor is a VCS question, not arithmetic for the caller**: Git has no ordering to count back along - it is the commit's *first* parent, and for a merge any other parent reports the lines the merge brought over as though the merge had written them - while SVN records no parents at all (`VcsLogEntry.ParentRevisions` is empty for it) but numbers revisions globally, so N-1 is the state of the repository before N whether or not N-1 touched the file. So `GetPreviousRevision` is on `IRevisionControlSystem`, answered once per system. Null means nothing came before, and the caller reads that as an empty left-hand side rather than a failure - the first commit of a repository and a file the commit added reach the same place by different routes. **The four existing tests passed unaltered against the new behaviour** - `SidesFor` takes two strings and does not know what they mean - while their names and prose described a dialog that no longer existed, so they were rewritten rather than extended. Twelve new tests, and the merge one is the only one that can tell first parent from last: with a single parent, `FirstOrDefault` and `LastOrDefault` are the same mutant. |

### Test fidelity — from the whole-solution mutation audit

Opened 2026-09-19 from `build/run-mutation.ps1 -All` over all seven measured assemblies (5h 40m;
23,634 mutants run, 5,990 survived, 4,159 in `MLQT.Shared` code no test executes at all — which is
the coverage ratchet's existing, reasoned debt and not a finding here). What *is* a finding is
below, and it has one shape: **the invariants this repository documents most carefully are the ones
with no test behind them.** Each line cited carries a comment explaining why the guard matters, is
executed by the suite — so the coverage gate is satisfied — and can be deleted or inverted without a
single test objecting. Regenerate the survivor list with `-Summarise`; it is git-ignored.

**Re-run 2026-09-22, and the figures above are not comparable with it** — 52m 53s, 24,901 mutants,
11,343 survivors. **B267 is why**, and the change was the right one: before it, Stryker ran in
solution mode and judged a mutant by *every* suite that transitively references the mutated
assembly, so the projects at the bottom of the dependency graph were being marked by tests in
projects above them. The pre-B267 reports still on disk prove it — `ModelicaGraph` was mutated
against **3,218** tests where its own suite has 995, and `MLQT.Services` against **2,305**. Only
`MLQT.Cli` and `MLQT.McpServer` had the same suite either way (nothing references them), so only
those two rows can be set side by side. Everything else got harder to pass, which is the point: the
question is whether *this assembly's own tests* check it.

**The per-project result, separating code no test runs from code the tests run and do not check** —
the second is the number worth acting on, and `build/survivor-map.py` groups it by method:

| Project | Mutants | Unreached | Reached | Survived | Kill on reached |
|---------|--------:|----------:|--------:|---------:|----------------:|
| ModelicaParser | 7,034 | 250 | 6,784 | 1,229 | **81.9%** |
| ModelicaGraph | 2,512 | 196 | 2,316 | 620 | 73.2% |
| RevisionControl | 1,606 | 340 | 1,266 | 383 | 69.7% |
| MLQT.Cli | 1,360 | 0 | 1,360 | 438 | 67.8% |
| MLQT.Services | 3,756 | 535 | 3,221 | 1,039 | 67.7% |
| MLQT.McpServer | 2,630 | 356 | 2,274 | 847 | 62.8% |
| MLQT.Shared | 6,003 | **4,375** | 1,628 | 735 | 54.9% |

`MLQT.Shared`'s 4,375 unreached is the coverage ratchet's existing, reasoned debt and not a finding
(and the browser journeys, which reach a great deal of it, are not in a mutation run at all). Its
14.9% headline is that debt; 54.9% is what its own tests do with what they execute.

**The two worst ratios in the whole campaign are non-findings**, checked before being reported:
`MLQT.Shared/Models/AppSettings.cs` (152 survived, 7 killed) is 127 mutations of **hex colour
literals** in theme presets, and `MLQT.McpServer/Helpers/ConnectorColor.cs` (40 survived, 1 killed)
is 40 string mutations in a colour table. Both are the "code nobody should test" shape CLAUDE.md
names. Reading the ratio alone would have opened two items worth nothing.


| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B219 ✅ | **Nothing notices if an external-stub write/report guard is removed** | Checking | ⭐⭐⭐ | S | `LibraryCheckSession.cs:44` and `FormattingPipeline.cs:144-145` (`SourceType != EncryptedDirectory`, `!ReferenceOnlyScope.IsReference`) could each be inverted with nothing objecting — every test asserted the *included* outcome, so the thing that should have been excluded was never in the list. `ExternalStubWriteGuardTests` now asserts the excluded outcome with its positive control in the same test, and the three `Where` clauses moved to `FormattableLibraries` so the write guard is answerable without running a hundred-line save. Two corrections to the original row: **`GraphAnalysisContext.cs:47` does not belong here** — its survivor forces the filtering branch, which filters a list with nothing to filter, and the guard itself is tested; and `LibraryCheckSession.cs:44` **still survives**, because `StyleCheckRunner` filters stubs again downstream, so removing the first of two filters changes no observable result. |
| B220 ✅ | **The formatting-exclusion decisions are covered but unchecked** | Rules / formatting | ⭐⭐ | S | One of the three was real. `StyleCheckingSettings.cs:310` caches the compiled exclusion patterns, and the mutation making it ignore an edit survived `EditingTheListTakesEffect` — because that test goes empty → one → empty and `IsLibraryExcluded` returns on an empty list before the cache is consulted. Replacing one entry with another is what reaches it, and is what a user does when correcting a name in Settings; three tests now cover replace, grow and shrink, and the mutant is dead. **The other two were equivalent mutants**: `FormattingExclusion.cs:57` and `CoverageDimensions.cs:147` are short-circuits in front of a slower answer that agrees, so forcing the slow path changes nothing. Recorded because "a survivor is a missing test" is the wrong reading, and this row asserted it. |
| B221 ✅ | **Nothing notices if a parse diagnostic becomes suppressible or baselineable** | MCP / CLI | ⭐⭐ | S | "Always reported, never configurable, never baselined" was enforced in four places and tested in none. `SuppressionTools.cs:64` refused a diagnostic and no test ever passed it one, so both the `\|\|` and the `"MLQT."` prefix could be emptied — a theory over five ids, bare and prefixed, now asserts the refusal *and* that the source is untouched. `StyleTools.cs:286` passes `honorSuppressions: true`; flipping it made check_library report findings the user had waived, which is worse than noise because an agent will go and "fix" them. `MetricsRecorder.cs:92` excluded diagnostics from the recorded debt count, and the `&&`→`\|\|` mutant survived because no test had a diagnostic in the set. All four mutants confirmed dead. `Baseline.cs:191/267` is left open: its survivors change whether an empty `ExcludedLibraries` is written as `[]` or omitted, which is a serialisation shape rather than a guard. |
| B222 ✅ | **`ImpactAnalysisService` computed an SVG layout nothing read** | Dependencies | ⭐⭐ | S | Deleted: `CalculateForceDirectedPositions` and `CalculateEdgeEndpoints` (about 165 lines of circular placement, deterministic jitter, a 50-iteration force relaxation and clamping), `NetworkNode.X`/`.Y`, `NetworkEdge.X1`–`Y2`, `ImpactAnalysisResult.SvgWidth`/`.SvgHeight` and the two `svgWidth`/`svgHeight` parameters — 243 lines net. **Nothing in the product referenced any of them**: the build broke only in the test project, which is the proof. The three tests that kept it alive are gone too; each looped over a collection asserting a bound the code could not violate (`node.X > 0` against a clamp of 40, `SvgWidth >= 700` against `Math.Max(700, ...)`) and would have passed on an empty result. Replaced by two that can fail — every edge end is a node in the same result, and the node set is exactly the selected plus impacted models, by count as well as identity. |
| B223 ✅ | **The read-only-settings guard on a repository is unchecked** | Repositories | ⭐⭐ | S | `RepositoryService.cs:634` sets `IsSettingsReadOnly` when `.mlqt/settings.json` cannot be written, and `:317` is the only thing that reads it — the warning telling the user their per-repository settings are being ignored. Both could be removed silently; both mutants are now dead. Tested by making the write fail portably (a *file* where the `.mlqt` directory must go, so `Directory.CreateDirectory` throws the `IOException` the guard catches) with an ordinary repository as the positive control. `:241` stays as it is: its survivor changes only whether a log line is emitted. |
| B224 ✅ | **The test suite committed to the MLQT repository itself** | Test harness | ⭐⭐⭐ | S | Running `run-mutation.ps1` over `MLQT.Services` made `RepositoryServiceTests` create two branches in the MLQT checkout, commit to one as `MLQT User <user@mlqt.local>`, detach HEAD and discard every uncommitted change in the working tree — twice, on 2026-09-18 and again on 2026-09-19, the first time silently moving two days of commits onto a branch nobody created deliberately. The tests build a real git repository under the temp directory and are correct; what was missing was any statement that the service still had hold of it. A string in `RepositoryService` mutated to `""`, git fell back to the process working directory, and that is the checkout. **This generalises**: any mutated code doing file or VCS work can act on the repository the run started in. Fixed by `SandboxedId`, which proves the repository is under `Path.GetTempPath()` before returning its id, on all 61 call sites, with `SandboxedId_RefusesARepositoryOutsideTheTempDirectory` as its positive control. The SVN tests against a fixed path outside the sandbox are the deliberate exception and stay unguarded — recorded at the helper. |
| B225 ✅ | **`--force-with-lease` was promised in two interfaces and pinned by nothing** | Git | ⭐⭐⭐ | S | `GitRevisionControlSystem.cs:1597` builds `push --force-with-lease`, `IRevisionControlSystem.ForcePush` and `IRepositoryService.ForcePushAsync` both document the lease, and the string appeared in **no test in the solution** — the mutation audit could replace it wholesale unnoticed. Dropping to a plain `--force` passes every other test here and silently deletes a colleague’s pushed commits the first time two people share a branch. Now tested as behaviour rather than as a flag: with a bare remote and a second clone that pushes first, the force push must fail **and their commit must still be on the remote** — the second half is what a plain `--force` breaks. A positive control covers the un-diverged case, and swapping the flag was confirmed to fail the test. |
| B226 ✅ | **The formatter could overwrite a file it had failed to parse** | Formatting | ⭐⭐⭐ | S | `IncrementalFormatter.cs:143` returns rather than writing when the render reported parse errors — "reformatting invalid Modelica produces unreliable output, and this overwrites the file in place". Deleting that `return` broke no test: nothing had ever handed `FormatAndWriteAsync` a file it could not parse, on the path that runs **at startup and after every VCS operation**. `IncrementalFormatterWriteTests` now covers it, with the formatted case as its positive control and a third test holding the scope of the guard — one unparseable file must not stop the others being formatted, or a single syntax error silently switches formatting off across the repository. |
| B227 ✅ | **`ModelicaRenderer`: 361 surviving mutants, and it rewrites every line of a user’s library** | Formatting | ⭐⭐ | L | 2,393 mutants run, 85% killed — a good rate over much the largest body of decisions in the solution, and still the biggest absolute count anywhere in the audit. Each survivor is one layout decision the suite executes and does not depend on, in the code that rewrites a library in place. Not a defect list and not a score to chase: the work is to read them in groups (indentation state, section ordering, annotation placement, line breaking) and decide per group whether the behaviour is specified anywhere. Expect a meaningful share to be equivalent — two spellings of the same output. **Do not start this without a way to sample**; 361 read end to end is how a pass like this turns into tests that assert the renderer’s current output rather than its contract. **✅ Done 2026-09-22, and the answer to “is this behaviour specified?” was mostly no.** The sampling the row insisted on is `build/survivor-map.py`, which groups a Stryker report’s survivors by the method they sit in — so a group can be judged as a whole instead of 272 mutants being read end to end. Re-measured first, because this row’s figures were from the WP8 audit: **272 survivors at 85.38%**, not 361. The map put 84 of them in four methods. Of those, **two groups had something written down to test against** and were taken: the file’s shape (`VisitStored_definition` — the within clause, a blank line between classes and none after the last) and section merging and ordering (`VisitComposition`/`WriteComposition`/`WriteInitialSections`, all six switches of which `Documentation/code-formatting.md` describes). **The largest single group was left deliberately**: the six-condition heuristic in `VisitClass_or_inheritence_modification` that decides when a modification goes multi-line counts arguments and nesting depth against thresholds nobody chose (`> 5`, `>= 2`), and a test for those would freeze today’s bytes under the name of a contract — which is the failure this row was written to prevent. Only its one stated rule, *break a line that would exceed the maximum*, is now held. **Why the file-shape group was unguarded is the harness, not an oversight**: `TestHelpers.AssertClass` ends with `actualOutput.RemoveAt(0)` and strips trailing blank lines, so every renderer test written through it discards the within clause and the blank lines before asserting; and it has no parameter for `InitialSectionsLast`, so no test could set that option at all. The new tests render directly. **Two real findings**: B273 (`within ;`), and six `WriteComposition` call sites passing an argument that is only ever read for a protected section — removed, with the one protected call that does need it named at the call site. Measured: **272 → 256 survivors, 85.38% → 86.01%**. The small move is the honest result: what is left in `VisitStored_definition` is index arithmetic over a shape the grammar guarantees, and most of the rest is layout nobody has ever specified. |
| B228 ✅ | **`ModelicaFileEncoding` loses a byte-order mark, and can ignore an explicit encoding** | Parser | ⭐⭐ | S | Four survivors that are not equivalent. `:132` inverts `!File.Exists`, so an existing file is written as `Default` instead of in the encoding it was read in — harmless for the bytes, because the fast path is only taken for ASCII text, but a **UTF-8 BOM is dropped**, which is a change to a file MLQT was asked to preserve. `:112`, `:168` and `:175` are `encoding ?? …` null-coalescing mutations: an explicit `encoding` argument can be discarded with no test objecting. The `IsAscii` survivors on `:154`/`:158` are **not** part of this — they push onto the slower path that agrees, exactly as the method’s own comment says. **✅ Done 2026-09-21, and the row's line numbers were stale - the survivors were re-read from the tool B267 had just fixed rather than trusted.** Two of the four named were still alive: the `!File.Exists` guard in `EncodingToWrite` (`:250`), where an existing file takes the new-file answer and a UTF-8 BOM is silently dropped, and the **synchronous** `encoding ??` (`:114`) - the asynchronous half was already covered by `AnExplicitEncoding_IsUsedInsteadOfTheFilesOwn`, and only that half. The third `??` is a *newline* rather than an encoding one and was already dead, because every write goes through `ForFile`. **The real gap was one line further in**: `DominantNewline`'s counting loop had no coverage at all - its comparison, its increments and its `crlf > lf` answer could each be changed with nothing objecting - because nothing tested `ForFile` directly, only through the encoding tests, which never vary the endings. Measured either side on the same command: **36 survivors before, 26 after**, the ten killed being both targets plus the whole of that loop. What is left is equality mutations in `DetectFromBytes`'s byte comparisons and block removals in catch handlers, which is the shape the row said to expect. |
| B229 ✅ | **`DymolaCheckingService` and `OpenModelicaCheckingService` are the least-guarded code in the solution** | External tools | ⭐⭐ | M | 11% of covered mutants killed in each — 130 and 122 run, 116 and 108 surviving. They are the only two services whose suites **no CI job runs** (`DymolaInterface.Tests` and `OpenModelicaInterface.Tests` need a live tool), so nothing outside one developer’s machine exercises them at all, and the mutation audit measured what that is worth. These parse a tool’s output into findings the user then acts on, so a survivor here is a misreported check result rather than a cosmetic one. Needs a live Dymola or `omc`; `build/run-all-tests.ps1` is the only thing that runs them today. Note B116 is already open against `omc` 1.26 on the same suite. **✅ Done 2026-09-21, and it needed no live tool at all.** The premise was that these are “the only two services whose suites no CI job runs” — but the services live in `MLQT.Services` and are tested by `MLQT.Services.Tests`, which CI does run. It is `DymolaInterface.Tests` and `OpenModelicaInterface.Tests`, the *interface* suites, that need a tool. **What actually stopped the services being tested was a type in a signature**: both factories returned the concrete session class, every method of which ends in a round trip to a running tool, so nothing past the first call was reachable without one. Extracting `IDymolaInterface` (5 members) and `IOpenModelicaInterface` (4) — exactly what the checking services call — made the whole of both reachable. **The tests are one contract run against both tools** (`MLQT.Services.Tests/ModelCheckingServiceContract.cs`, 48 behaviours × 2, with `ToolHarness.cs` holding fakes that model each tool’s **log buffer** rather than returning fixed strings — most of these promises are about which check’s output a result carries, and a stub cannot tell a drained buffer from an undrained one). Measured: **11% killed became 73.6% and 73.5%, and 176 mutants covered by no test became 13**. **It found two defects, both in both tools** (B272): the package path had its own copy of the check body, without the drain-the-log-first step or the read-it-on-success step, so B170’s warnings were shown when you checked one class and lost when you checked its package, and an error could be reported against the class after the one that produced it. Fixed by making `CheckSingleModelAsync` the only place a check happens. Of the 52 survivors left, 36 are log-call text and the other 16 are throttle timing and two equivalent null checks — read and recorded at the test class, as CLAUDE.md asks. |
| B255 ✅ | **Two WP3 tests only held on the platform they were written on, and CI is where that showed** | Testing | ⭐⭐ | S | The Linux `build-libraries` job failed on the push at the end of WP3; both Windows jobs and everything else were green. **Two tests, one shape**: an assumption about the machine, invisible on the machine that made it. (1) `ModelicaPackageSaverTests.SaveLibraryToDirectoryWithResult_DoesNotReformatAnExcludedModel_EvenWhenFormattingWouldChangeIt` asserted the written file was **CRLF throughout**, and its own comment explained why - the fixture is a raw string literal, so it carries the endings of the test file it sits in. Those are CRLF on a Windows checkout and LF on the runner, where `core.autocrlf` is off. **The product was right on both**; the test named an ending B251 had deliberately made the platform's business. Now asserts the property B251 actually claims - one ending throughout, whichever - through a helper that says nothing about which. (2) `PackageSplitterTests.IfTheOldFileCannotBeDeleted_TheUserIsToldTheyHaveItTwice` made the delete fail by holding the file with `FileShare.None`. **Four other tests in this repository do the same thing and are right to**: .NET implements `FileShare` on Unix as an advisory lock that other .NET `FileStream`s honour, so blocking a *read* works on both platforms. Deleting is not a read - `unlink` ignores advisory locks, so the delete simply succeeded and the test asserted failure where the product reported success, which is the one outcome that test exists to catch. It now puts a directory in the file's place, which makes `File.Delete` throw everywhere; the splitter takes the path from the graph rather than from disk, so nothing before the delete notices. **Neither could be reproduced locally** - no WSL or Docker on the development machine - and an attempt to simulate the first by converting the test file to LF did *not* reproduce it, which is worth knowing: the simulation was wrong and the fix had to be made unconditionally true instead of true-on-both-measured-platforms. |
| B256 ✅ | **`ubuntu-latest` becomes Ubuntu 26 on 19 October 2026, and Playwright ships no browser for it** | CI / testing | ⭐⭐⭐ | M | Flagged by an annotation on every Linux job in the run of 2026-09-20: *"The ubuntu-latest label will migrate to Ubuntu 26 beginning October 19, 2026."* Six jobs across three workflows use the floating label - `linux-tests`, both `ubuntu-latest` legs of the `ui-journeys` and `desktop-self-test` matrices, `nightly-webkit`, and the `.deb` build in `release.yml`. **This is not a routine image bump, and the analysis is already written down**: `skill-gui-testing.md` records that Playwright ships no browser build for Ubuntu 26.04 at all - `install` refuses outright and the newest platform it knows is `ubuntu24.04-x64` - and that with `PLAYWRIGHT_HOST_PLATFORM_OVERRIDE=ubuntu24.04-x64` Chromium runs and **WebKit still will not launch**, because its build links `libicu74` and `libvpx9` while 26.04 ships `libicu78` and no `libvpx9`. That was measured for B135 on a developer machine; the date turns it into a CI problem. **The nightly WebKit rehearsal is the one that has no workaround** - it exists precisely because WebKitGTK is what the Linux desktop host runs on, so losing it means the Linux GUI is rehearsed nowhere. `build/run-all-tests.ps1` already detects a too-new Ubuntu and applies the override, held by `TestRunnerScriptTests`; **no workflow does**, so CI has none of that protection. Three decisions, and they are separate: pin the runner image to `ubuntu-24.04` (buys time, and the label is the only thing that moves - but a pin nobody revisits is how a project ends up on an unsupported image); apply the override in the workflows as the script does, which keeps Chromium and not WebKit; or find what WebKit needs on 26.04. **Also check the `.deb`**, which is a different question with the same trigger: built on 26 it would declare dependencies against that release's libraries, and MLQT's users are not all on it. **Do this before 19 October, not after** - the failure arrives on a date rather than on a commit, so it will land on whatever push happens to be next and will read as that change having broken something. **✅ Pinned 2026-09-21, four weeks before the date.** All six jobs across the three workflows now name `ubuntu-24.04`, and `WorkflowPlatformParityTests.NoJobRunsOnTheFloatingUbuntuLabel` refuses a new one that goes back to the label - the pin is the kind that holds until somebody adds a job, and the failure it would cause arrives on a date rather than on that commit, so it would land on whatever push happened to be next and read as that change having broken something. Two existing parity assertions had the label written into them and now look for an Ubuntu runner rather than a particular version, so repinning does not break them. **The choice was the pin rather than the override**, which keeps Chromium and cannot bring WebKit back, or a fix for 26.04, which nobody has: the object is to finish the backlog and cut a release, and the pin is what makes both possible without changing what is rehearsed. It is not a fix - revisit when Playwright ships a 26.04 browser, or by April 2029 when 24.04 leaves standard support, whichever comes first. **The `.deb` needed nothing.** Its `Depends:` is hand-written and unversioned - `package-deb.sh` refuses `dpkg-shlibdeps` so the package is not pinned to its build host - nothing in the payload is compiled there, and the archive is `-Zxz` rather than the host default, so the floor is `libwebkit2gtk-4.1-0` + GTK3 (Ubuntu 22.04, Debian 12) wherever it is built. What a newer builder would have cost is not the package but the proof: the release job extracts what it built and runs the 16 `/selftest` probes against it, and that needs a WebKitGTK that launches. |
| B267 ✅ | **`run-mutation.ps1` cannot mutate `ModelicaParser` any more: its baseline run includes the browser journeys** | Test harness | ⭐⭐ | S | Found on 2026-09-21 doing WP7. `./build/run-mutation.ps1 -Project ModelicaParser -Mutate '**/Comparison/*.cs'` never reaches the mutation phase: Stryker runs an initial test run of **5,790 tests** — more than `ModelicaParser.Tests`'s 2,084 — and aborts with *"Initial testrun has more than 50% failing tests"* naming `MLQT.Journeys.TabNavigationJourney.MovingThroughEveryTabAndBack_RaisesNothing` as `error`. **That journey passes on its own** (`dotnet test MLQT.Journeys -c Release --filter TabNavigationJourney`, 7 passed), so this is about the run, not the journey. The cause looks structural rather than environmental: Stryker is in solution mode (*"Identifying projects to mutate in MLQT.slnx"*), and in solution mode it runs **every test project that transitively references the mutated one** — which for `ModelicaParser` is all of them, journeys included — apparently regardless of the `--test-project` the script already passes. **The arithmetic confirms it**: 5,790 is the seven core suites' 6,412 less `RevisionControl.Tests`'s 689 — the one suite that references nothing — plus the journeys. The journeys share one host, so running them alongside six other suites is exactly the shape CLAUDE.md warns about. **WP9, and first in it**: B227 and B228 are lists of surviving mutants and every closure in B219-B226 was recorded as "mutant confirmed dead", which is a claim the tool has to be able to make. **Two candidate fixes, and which one is right is the question**: give Stryker the project file rather than the solution so it stops enumerating, or teach the script to exclude the journeys from the test set. Until then the audit and every per-file run over `ModelicaParser`, `MLQT.Services` or `MLQT.Shared` is unavailable, which is the tool CLAUDE.md names as the answer to "do the tests actually check anything?". **✅ Fixed 2026-09-21, and neither candidate above was quite right.** A solution cannot be taken away by argument, only by not standing where it can be found: Stryker walks up from its working directory, and from the repository root it finds `MLQT.slnx`. `Invoke-Stryker` now runs from the test project's own directory, where no solution is found, Stryker takes the single-project path and uses the suite it was started from. Measured on the same command that failed: *Analyzing 1 test project(s)*, **2,078 tests rather than 5,790**, and a mutation score in 2m32s where it previously aborted after 3m30s. `--test-project` is **removed** rather than kept in case - it did nothing in the case that mattered, and a flag that appears to select the suite while the working directory actually selects it is worse than no flag. The output path is resolved to an absolute one before the directory changes, or a relative `-Output` would land inside the test project. `-All` uses the same function and is unchanged; it was not re-run, because it takes hours and the path it exercises is the one just measured. |
| B268 | **A superseded library goes on claiming the classes it no longer supplies** | Libraries | ⭐⭐ | M | The root cause behind the lookup B191's fix had to compensate for. A library loaded twice — the user's checkout and a tool's encrypted build of the same library — races to fill one graph: `DirectedGraph.AddNode` resolves each class in favour of readable source, but **the library index is never told**, so the losing `LoadedLibrary` keeps the id in its `ModelIds`. The consequences are already documented in two places without being fixed in either: `ILibraryDataService.TotalModelCount` exists only because summing `ModelIds` over-counts and did so *differently on each launch* (77,860 then 76,129 for one project), and `DictionaryScope.RepositoryForModel` carried a half-workaround that skipped claimants with no repository — which does not help when both have one. `LibraryOwnership.Owner` now answers "who owns this class?" correctly by asking the graph which copy survived, and five callers go through it, so **nothing is currently wrong because of this**. What is left is that the index still says something untrue, so the next caller to write `Libraries.FirstOrDefault(l => l.ModelIds.Contains(id))` gets the old bug back and no test objects. Two candidate fixes: have the load that wins a collision withdraw the id from the loser's index, or stop `ModelIds` being a claim at all and derive it from the graph. **Measure the second before choosing it** — `ModelIds` is read on hot paths. **A third candidate arrived from reading `MainLayout` while fixing B269, and it removes the race instead of tolerating it.** There are two routes by which reference material is loaded, and only one of them is ordered. The `ReferenceLibraries` setting is loaded by `LoadReferenceLibrariesAsync` **after** the project's own repositories, and the comment above that call says why: *"a tool's library folder ships encrypted builds of libraries the user may have checked out as source, and the source copy must win"*. A **reference-only repository configured in the project** takes the other route — it is one of the repositories `LoadRepositorySettingsAsync` loads, in the same parallel pass as the user's own checkout, with no ordering at all. That is the route the reported project used: its four repositories include `C:\Program Files\Dymola 2026x Refresh 1\Modelica\Library`, and the encrypted `Suspensions` finished at 17:06:27 while the source `Suspensions` finished at 17:06:37. **So the ordering rule is narrower than its comment claims**, and making the second route obey it would mean no class is ever stubbed before its source arrives — which is what makes both indexes true, rather than making one of them readable. **WP15**, whose whole content this is. |
| B271 ✅ | **`CodeReview.OnModelSelected` calls `StateHasChanged` off the dispatcher** | Code Review | ⭐⭐ | S | Found 2026-09-21 while doing B237, by the journey that item exists to make possible - which is the argument for the journey rather than against it. Under the journeys the circuit dies with `System.InvalidOperationException: The current thread is not associated with the Dispatcher. Use InvokeAsync() to switch execution to the Dispatcher when triggering rendering or component state.`, and the frame below the assert is `CodeReview.OnModelSelected` at `CodeReview.razor.cs:859` - the bare `StateHasChanged()` after `_isLoadingCode = true`. The same method marshals correctly a few lines above, inside the VCS branch, so the two halves of one method disagree. **The first question is whether a user can reach it**, and it is not answered: the stack arrives through `RendererSynchronizationContext.PostAsync`, so this may only be a continuation resuming after the circuit was already in trouble rather than the first fault. Settle that before changing anything - `OnModelSelected` is reached from the findings row click (on the dispatcher) and from `AppState.OnChangeModel`, and the second is raised from background analysis completions, which is the path that would make it real. CLAUDE.md already states the rule this is on the wrong side of: *in Razor event handlers, use `await InvokeAsync(StateHasChanged)`*. **✅ Withdrawn 2026-09-21: not a defect, and the row above it was wrong.** Filed from the stack alone before the reachability question it set itself had been answered - and the answer is no. `AppState` is a singleton shared by every circuit, the journeys opened 71 pages and closed none of them, so every stale page's `CodeReview` reacted to a model change raised by a later page **on that page's dispatcher**. The bare `StateHasChanged()` calls at `:824` and `:859` are the method's synchronous prologue and are correct where there is one circuit: the desktop host has one, and every `ChangeModelID` caller is on the dispatcher or wrapped in `InvokeAsync` already. Nothing in `CodeReview` was changed. The fix was in the harness (`NewPageAsync` closes the previous page) and it is B237's. **Kept rather than deleted because the mistake is the point**: a stack trace says where a thing failed, not whether a user can reach it, and this one was filed as a product defect on that evidence. |
| B272 ✅ | **A package check lost the warnings a single-class check showed, in both tools** | External tools | ⭐⭐ | S | Found by B229’s contract tests, fixed with them, recorded because the shape recurs. `CheckModelAsync` and `CheckSingleModelAsync` were two copies of the same check body in each service, and the copy the **package** path used had neither the clear-the-log-first step nor the read-the-log-on-success step. So `checkModel` returning true with six warnings — the exact case B170 exists for — surfaced them for a class checked on its own and dropped them for the same class checked as part of its package; and because the log was never drained between classes, an error reported against one class could be the one before it produced (Dymola’s log accumulates; omc’s empties on read, so there it leaked from a class that passed silently into the next one that failed). `CodeReview.razor.cs`’s `ReportedChecks` was already written for the fixed behaviour — it lists *“any clean check the tool still had something to say about”* — so the feature had been specified, shipped and never delivered on that path. **Neither half is visible from either path alone**, which is why the guard is a contract asserted once and run against both tools rather than a test per service. |
| B273 ✅ | **Every top-level `package.mo` MLQT wrote said `within ;`** | Formatting | ⭐⭐ | S | Found by B227’s first new test and fixed with it. `WithinClause` is, per CLAUDE.md, **the only place that adds or removes a leading within clause**, and it writes `within;` for a top-level file. The renderer wrote `within ;` — `Space()` was called before the optional name rather than with it — and `ModelicaPackageSaver` re-renders the whole `stored_definition` on every save, so the renderer’s spelling is the one that reached disk and the helper’s never did. Valid Modelica and stable (it re-parses to itself, so no churn loop), but it is a one-character diff on the top-level `package.mo` of every library the first time MLQT formats it, and a silent divergence from the single place the rule was supposed to live. **Nothing saw it because the only assertion about a within clause anywhere was `Assert.Contains("within", ...)`** — which the name, the space and the semicolon can all go missing under — and because `TestHelpers.AssertClass` deletes the within line before comparing. |
| B274 ✅ | **The MCP edit tools move and delete classes in a user's repository, and 319 mutants say nothing checks how** | MCP | ⭐⭐⭐ | M | From the 2026-09-22 campaign, by the second question WP8's audit asked — *which survivors sit on code that writes to a user's files?* `EditTools.cs` has **179 survivors** and `StructureEditTools.cs` **140**, and these are the operations an AI agent invokes to restructure somebody's library: `MoveClass` (40), `DeleteClass` (18), `DeleteDirectoryPackageAsync` (14), `MoveDirectoryPackageAsync` (13). The shape inside `StructureEditTools` is worse than the count: `RemoveComponent` (16) and `RemoveWholeLine` (10) survive mostly **equality and arithmetic** mutations — index arithmetic over the lines of a file that is about to be rewritten, where an off-by-one deletes the wrong line and the tests do not notice. B225 and B226 were this category and both were treated as same-day fixes; this is the same category, measured, and larger. **WP9.** **Start with the removal arithmetic, not the message strings** — a third of these are `String mutation` on tool responses, which matter much less than which line got deleted. **✅ Done 2026-09-22.** Two structural findings, both the duplication shape rather than a defect — the arithmetic was right, nothing depended on it. **(1) The line removal existed twice in one file**: `RemoveComponent`'s sole-on-line branch computed the same three offsets as `RemoveWholeLine` seven lines away, so the code deciding which characters of a user's file disappear had two copies and a fix to either was a fix to one of them. Now one, returning null where it used to fall back to `stop`, so `RemoveConnection` keeps the stricter answer `RemoveComponent` already gave. **(2) The subtree test existed four times**, always as `id == root \|\| id.StartsWith(root + ".")`, and the `"."` could be emptied in every copy — which makes `Root.Src` a prefix of `Root.SrcExtra` and sweeps an unrelated package into a move or a delete. All four now go through `ModelicaName.IsInSubtree`/`ReRoot`, **added to the helper that already existed for exactly this reason** and whose own summary already said so: *“three lines of string arithmetic, written out at every site that needed them… that is the sort of thing that stays right until one copy is edited.”* **The tests are the contract, not the count**: what removal leaves behind is *the original lines minus exactly one* (which catches swallowing the line above, leaving a blank line, and merging two lines — none of which change whether the removed text is gone, which was all the tests before them asked), and a refused move is asserted **by message** with the library unchanged, because `Move_Validation` asserted only `IsType<ToolError>` and a mutant that removes the guard still errors further down. Measured: `StructureEditTools` **140 → 128** survivors, `EditTools` 179 → 175 over 13 fewer mutants, and the relocated logic is **not** hiding in a blind spot — `ModelicaName`'s three new members have **zero** surviving mutants under `ModelicaParser.Tests`, which was checked rather than assumed. The message-string survivors are left, as the row asked. |
| B275 ✅ | **`Push`, `Commit` and `ForcePush` have 64 surviving mutants between them** | Revision control | ⭐⭐ | M | From the 2026-09-22 campaign. `GitRevisionControlSystem.cs` has **228 survivors** and `SvnRevisionControlSystem.cs` **139**, and the concentration is in the operations that change a user's history and their remote: `Push` (24), `Commit` (22), `ForcePush` (18), then `GetWorkingCopyChanges` (13) and `AbortRebase` (13). Unlike B274 these have a real reason behind them — `RevisionControl.Tests` is 689 tests against temporary LibGit2Sharp repositories and a local SVN server, and pushing needs a remote nothing has — so the honest split is **which of these can be tested against a bare repository on disk** (a push to a `file://` remote can; force-push and its refusals can) and which genuinely cannot. `SvnRevisionControlSystem` also carries **205 unreached** mutants, which is the three svn-dependent classes CI excludes and is already reasoned debt rather than a finding. **WP9**, after B274. Do the Git half first: it needs no server. **✅ Done 2026-09-22, and the split came out three ways rather than two.** **(1) Testable, and the gap was real.** `Push` had no positive test at all — an invalid path and a repository with no remote were the whole of it, so nothing established that a push puts anything anywhere — and no test that a **non-fast-forward push is refused and the other person's commit survives**, which is the safety property separating `push` from `push --force-with-lease` and which force-push already had a test for. Both now exist, against a bare repository on disk, using the fixture the force-push tests already established. `Push` **24 → 18** survivors. `Commit` gained a test that an untracked file really is in the commit — `Commit_WithAllChanges_StagesAndCommits` created one but asserted only that a commit came back, and it modified a tracked file too, so turning off `IncludeUntracked` left it green while every newly created class silently failed to be committed. **(2) One target turned out to be an equivalent mutant**, checked by hand rather than assumed: `RecurseUntrackedDirs = false` in the staging block changes nothing, because `Commands.Stage` on the directory path stages its contents whether or not the status listing expanded them. The test is kept — it states a real promise and it is what kills `IncludeUntracked` — but the mutant beside it cannot be killed and is recorded here rather than chased. **(3) What is genuinely left needs what no machine here has**: the rest of `Push`/`ForcePush` is error-message mapping from a `git` exit code, and authentication and network failures. `GitRevisionControlSystem` overall **228 → 221**. `SvnRevisionControlSystem` was not touched: its 139 survivors and 205 unreached mutants are the three svn-dependent classes CI excludes, which is reasoned debt already recorded in `build/coverage-baseline.json`. |
| B276 ✅ | **Ten more hand-written copies of “is this class inside that one?”** | Architecture | ⭐ | S | Found while doing B274, and deliberately out of its scope. The solution has **fourteen** sites spelling `id == root \|\| id.StartsWith(root + ".", StringComparison.Ordinal)`; B274 routed the four in `MLQT.McpServer/Tools/EditTools.cs` through `ModelicaName.IsInSubtree` and left the rest: `MLQT.Services/DymolaCheckingService.cs`, `MLQT.Services/OpenModelicaCheckingService.cs`, `MLQT.Shared/Pages/CodeReview.razor.cs`, `MLQT.Shared/Pages/MetricsDashboard.razor.cs` (×5) and `ModelicaGraph/Analysis/TypeResolver.cs` (×2). **Nothing is wrong today** — every copy is currently correct, and the two checking-service copies are held by `ModelCheckingServiceContract.CheckingAPackageChecksNeitherItselfNorItsNamesakes`. This is the shape this repository keeps rediscovering rather than a defect: a rule with fourteen implementations is a defect waiting for one of them to be edited. **Small, and a good first-thing-in-the-morning item**: the helper exists, the tests for it exist, and each site is a one-line substitution — but the two `MetricsDashboard` ones are scope filters where a namesake being included changes a number on screen, so check what each one means before replacing it rather than sweeping. **✅ Done 2026-09-22, and two of the ten were not substitutions at all.** All fourteen hand-written copies are gone — the only remaining match in production code is the sentence in `ModelicaName`'s own summary describing what it replaced. Three findings on the way: **(1) `TypeResolver`'s two sites were `IsInSubtree` and `ReRoot` fused**, not membership tests: each asked whether a name was under an import alias and then re-rooted it onto the import's target, in four lines that `ReRoot` does in one — `import SI = Modelica.SIunits;` making `SI.Voltage` mean `Modelica.SIunits.Voltage`. **(2) `MetricsDashboard` already had a helper**, `InScope`, carrying a remark that makes this item's whole argument — *“The dot matters. A scope of `Modelica.Blocks` covers `Modelica.Blocks.Sources.Sine` and must not cover `Modelica.BlocksExtra`”* — and **three of that file's five copies were asking the question inline instead of calling it**. So the row's “one-line substitution” was right for the count and wrong about the shape: the duplication was not only across files, it was around an existing helper in the same one. `InScope` now delegates to `IsInSubtree`, keeping its own empty-scope-means-everything rule, and the other four call it or `IsStrictlyInside`. **(3) `InScope` had no tests**, despite that remark; `MetricsScopeTests` gives it the namesake case it names, and mutation confirms **zero** survivors in it. The row's warning about the dashboard sites was worth having and the answer was cheap: what is left in that file is seven hex colour literals and one equivalent mutant (`IndexOf(ch, parent.Length + 1) < 0` to `<= 0` — the search starts past index 0, so it cannot return 0). The row also said “the two `MetricsDashboard` ones” in its advice while listing five; five is right. |
| B269 ✅ | **A large file in diff mode took the whole application down** | Code Review | ⭐⭐⭐ | S | Reported 2026-09-21: opening a large FMU model with the Code Review tab in diff mode left the red *"An unhandled error has occurred. Reload"* banner, and the page then reported the file was too large to display. Caught in the debugger as `leftEl.addEventListener is not a function` out of `diffViewer.js:initSyncScroll`, thrown back through `DiffViewer.OnAfterRenderAsync`. **A file too big for the O(m·n) diff renders a message instead of the two panes**, so `@ref` never runs and both element references stay default — but `OnAfterRenderAsync` asked only what the *view mode* was, not what had been rendered, and called the interop anyway. A default `ElementReference` still serialises to an object, which is truthy, so it also went straight past `if (!leftEl \|\| !rightEl) return;` in the script. **Three fixes, and the third is the one that matters**: `ShowsPanes` is now one property the markup's last branch and the interop call both ask; the script checks for `addEventListener` rather than for truthiness; and the call is wrapped, because scroll synchronisation failing is worth a line in the log and is never worth the window. The same crash was reachable through "No content to display", which is not a large file at all. |
| B270 ✅ | **Reloading after an error asked for a project while one was still open** | Startup | ⭐⭐⭐ | S | Found with B269, because B269 is what put the Reload button on screen. Blazor's error banner offers Reload for any unhandled exception, and in a webview host that rebuilds the component tree while the services stay where they are — they are singletons registered by `AddMlqtCore` in the host's `Program.cs`. `RunStartUpAsync` therefore ran a second time against a process that already had a project open: the user was shown **Select Project** with the previous project's packages still visible in the library browser behind the dialog, and answering it would have loaded a second copy of everything into one graph. `MainLayout.StartupAlreadyRan` now asks the surviving service how many repositories are loaded — deliberately not a flag on the component, since the component is the thing that was just replaced — and skips the sequence, re-syncing only the project name. Zero loaded repositories is a genuine first run *or* a reload of a session with nothing open, and re-running startup is right for both. |
| B257 | **The combined analysis step reports itself as "Analysing dependencies" for the whole of it** | Performance / UI | ⭐ | S | Reported 2026-09-20 as *"analyse dependencies took a lot longer although the style checking then seemed to be pretty quick"* - and measured, that is exactly what happens and nothing is wrong with it. The deferred steps can run **combined**, and in that path style checking is interleaved with dependency analysis rather than following it. End to end on the same library the same afternoon: separate path deps 81.1s and all style checking complete at **353.4s**; combined path deps 244.3s and complete at **321.1s**. The combined path is **32 seconds faster** and its dependency phase looks three times slower, because it is carrying the other half. **The label is the only thing wrong.** A user watching a four-minute "Analysing dependencies" has no way to know style checking is most of it, which is how a faster path comes to be reported as a regression - it was, here, until the like-for-like comparison was found (combined runs on 2026-08-17 took 289.0s and 274.3s at the same scale, so today was the fastest of the three). Name the step for what it is doing, or report the two counts separately within it. **WP13**, with B261: WP4 filed this from its own measurements, named it in its prose, listed it nowhere and shipped without it. |
| B258 ✅ | **Startup still stutters, and reading the code has found only things already fixed** | Performance | ⭐⭐ | S | Reported 2026-09-20 after B190: the window is responsive during dependency analysis and style checking now, but still stutters during the initial load, and the user connects it to the library browser being populated. **Three candidates read and all three were already handled**, which is why this is an item rather than a fix: the bulk load already wraps itself in `SuppressTreeDataChanged()` so 122 library loads announce **once**; `CheckForUncommittedChangesAsync` already does its working-copy query inside `Task.Run`; and `RefreshTreeItems` builds only the **top level**, not 39,860 nodes. **One thing found and not yet priced**: `RefreshDescendantParserErrors` walks `GetAllModels()` - 69,141 of them - on the dispatcher, and the answer is global while the field is per component, so N repositories compute the same set N times. Whether that is the stutter is unmeasured. `RefreshTreeItems` is now timed per step and logs when a refresh costs 50ms or more, which is how B253 was found after two wrong guesses - **run it once and let the log name the step**. Also unexamined: `OnLibrariesChanged` is *not* suppressed during the bulk load, so it fires 122 times, and `MetricsDashboard`'s handler does a `ReportableModels()` walk and a `LoadAllHistory()` disk read synchronously on the loader thread each time - cheap for the loader if that page is not mounted, unknown if it is. **✅ The log named it on the first run.** `Tree refresh for 'Claytex' took 1522ms (top level **1477ms**, annotate 0ms, parser errors 44ms, expansion 0ms)` - and many like it. 97% of a tree refresh is `GetTopLevelModelsAsync`, and inside that `PrepareModelForDisplay` **renders every top-level class's icon**: resolving each one's base classes and parsing them to do it, on the dispatcher, on every refresh. Nothing cached it because it could not safely be cached - `IconSvg` lived on `ModelNode`, outside the invalidation that `ModelicaCode`'s setter already does for `ParsedCode`, `Coverage` and `Suppressions`. **So it moved to where its siblings live**: rendered once per class, discarded with the rest when the code changes, with `IconRendered` separate from the SVG being null because most classes have no icon and “asked, and there is none” has to be as cheap to remember as an answer. `ModelNode.IconSvg` stays as a facade. Four tests, the three that matter being about **invalidation rather than speed** - a cache that is slow is a disappointment, one that keeps an answer past the question is a wrong icon on screen - verified by deleting the invalidation and watching three of four fail. **The parser-errors step is 44ms and was left**: it walks all 69,141 models for an answer that is global while the field is per component, so N repositories compute it N times, but at 3% of the refresh it is not what was reported. **Second round, on the same evidence.** With icons cached, the log showed the walk that had been 3% become the whole of a refresh: `Tree refresh for 'Library' took 873ms (top level 0ms, annotate 0ms, **parser errors 872ms**, expansion 0ms)`. `GetAllModels()` builds a 69,141-entry hash set and filters every node in the graph, **under the service's lock** - which is why the outlier lands on whichever tree asks while a load still holds it, and why the ones after it are 12-65ms. The answer is a property of the project and every repository's browser computed it separately, so it moved to `ILibraryDataService.ModelsWithDescendantParserErrors()`: worked out once, handed to all of them, dropped in `RaiseTreeDataChanged` - **whether or not the announcement is suppressed**, because a bulk load is exactly when it goes stale and the single announcement at the end must not be served a set built before the libraries arrived. Four tests, the load-bearing one being that loading another library rebuilds it, verified by removing the invalidation. **A mock had to be taught the new method**: Moq returns null for a reference type, and the fixture now says what the interface promises rather than the component defending against it. **Third round: the work moved off the dispatcher.** With both caches in, the first refresh after a load still spent about a second in `top level` - the icons genuinely being rendered, once, but on the thread the desktop host pumps window messages on. `GetTopLevelModelsAsync` and `GetChildModelsAsync` both **returned a completed task**, so awaiting them ran everything on the caller: the name had been promising something it did not do. Both now run on the pool, and both **prepare outside the lock** - deciding which library owns a class needs it, rendering that class's icon does not, and holding it through work that reaches into the graph and parses other classes is what made an unrelated `GetAllModels` wait 872ms. **One consequence handled**: two trees can now prepare the same class at once, so the rendered flag is set *after* the render rather than before - claiming it first would let the second caller see “already rendered” with no SVG and silently drop the icon. **That ordering is reasoned, not pinned**: a test was written for it, the inverted ordering passed it every time, and the window between the two writes is too narrow to hit on demand. What the test does hold is that eight simultaneous callers all get a usable answer, and the test says so rather than claiming more. **Fourth round, and the timestamps gave it away.** Four trees reporting 1,421ms, 1,423ms, 1,429ms and 1,420ms and **finishing on the same millisecond** are not four pieces of work - they are four concurrent passes over the same set. Every tree asks `GetTopLevelModelsAsync` for *all* top-level classes and filters to its own repository afterwards, so a project with four repositories prepared the same hundred-odd libraries four times, and because they ran together none of them found the icons the others were rendering. **Setting the rendered flag after the render made that worse**, which is the honest cost of that correctness fix: before it, three of the four skipped. Now one preparer runs at a time - the first pass does the work and the rest find it done, the same wall clock for a quarter of the effort. **And the instrumentation had become misleading**: `top level` is no longer time on the dispatcher, so a large number there stopped meaning a blocked window. The log line now separates what is measured on the UI thread from what is awaited off it, because a measurement that outlives the thing it was measuring is worse than none. |
