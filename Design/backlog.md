# MLQT Backlog

The working list. Every open item has an id, and **an id is never reused** — they are cited from
code comments, test summaries, build scripts and CI workflows, so a new item takes the next number
above the highest ever issued, whatever has since been closed.

**B1–B240 have been issued.** B1–B167 were opened between 2026-09-03 and 2026-09-17 by the
seventeen end-of-branch reviews of the CI/CD toolchain and by phases 7a and 7b; B168–B203 by the
first end-to-end pass over the Photino release on 2026-09-17; B204 while settling B198's layout
question, B205 while fixing B192, B206 while trying to reproduce B198, B207–B209 while confirming
B172, B210–B211 from the resource reports in
Claytex and VeSyMA, B212 from WP8, and B213–B218 on 2026-09-18 from the decision that WP2 opens with
`analysis-viewer-fidelity.md` Part II, and B219–B223 on 2026-09-19 from the whole-solution
mutation audit, with B224 from what that audit did to this repository while running, B225–B226 from a second pass over the survivors asking which of them sit on code that writes to a user’s files, and B227–B229 as what that pass deliberately left for WP9, B230–B232 on 2026-09-19 from running WP2 step 1, B233 from the limit B214 shipped with, B234 from noticing that B213's strongest test runs nowhere automatically, B235 from measuring B185, B236 reported from ordinary use on 2026-09-19, and B237 from a journey that could not be made reliable. Of B1–B167 all are closed
except the two carried forward below, and the table they lived in was retired with the phase design
notes on 2026-09-17 — git history has it if the reasoning behind one of those ids is ever needed.

New items start at **B241**. The watermark moves as items close, not only as they are opened: the id
guard checks that the ids *above* it run unbroken, so a closed row leaves a gap the moment it is
removed unless the watermark has advanced past it.

`MLQT.Cli.Tests/MarkdownTableTests.cs` holds this file's table structure and the uniqueness of the
ids. A blank line between two rows silently ends the table, and an unescaped `|` in a cell splits it
even inside backticks — both have happened, and neither is visible in review.

---

## Carried forward from phases 7a / 7b

Two items whose id is already in use elsewhere and whose work is not finished.

| # | Item | From | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B152 | **Twelve documentation screenshots are still photographs** | doc review 2026-09-11 | ⭐ | M | 41 of the 53 images are regenerated from the real UI by `DocumentationScreenshots`; the rest cannot be produced that way and are listed in CLAUDE.md and `skill-gui-testing.md`. Two of them need Dymola, six need an SVN server, one needs an SVN repository to render its settings section, one needs the merge dialog's ready-to-merge phase (which needs a VCS operation performed *in the application* to refresh working-copy status), and the rest show the window frame. **Not a defect** — recorded so nobody re-derives the list. Worth revisiting only if a fixture ever gains an SVN server. |
| B166 | **`check_library` once returned a different finding count on each run, and the variance has not reproduced** | 2026-09-17 | ⭐⭐ | M | The three causes found alongside it are fixed — the MCP call passed the wrong dictionary root, `modelsChecked` counted excluded libraries, and one `AnalyzeDependenciesAsync` call omitted the library roots so `modelica://` resolution depended on call order. The original 20,763 / 20,790 / 20,766 spread did **not** reproduce: three calls in one session and three with a full reload between them each returned 21,249. Two details weaken the original report — the reload path returns a **hardcoded** `affectedModelCount: 0`, so "no source change" was read off a literal; and the runs were scoped to `Modelica` alone inside a graph holding seven other libraries, which is a materially different shape. **Left open at low confidence.** If it moves again, capture the **per-rule breakdown of two adjacent runs** — a total says nothing about which rule moved. |

---

## Phase 1 — issues found testing the new build

Opened 2026-09-17 from a pass over the Photino release on both platforms. Mostly UI, not
exclusively. Grouped by area; the ids are in the order they were written down.

### Findings and the Code Review page

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B176 ✅ | **No way to search the code being reviewed** | Code Review | ⭐⭐ | S | Locating a parameter by name in a long class means scrolling. A search box over the rendered source, with match highlighting and next/previous, is what the page is missing to be usable on a real class. **Done**: a "Find in code" box beside the view buttons, a match counter, and up/down that step and wrap. The two halves search different things on purpose — the page finds the **lines**, over the text as the user reads it (tags stripped, entities decoded, so `<html>` finds the documentation string and `KEYWORD` finds nothing), and `CodeViewer` tints occurrences inside each token's own span, keeping its syntax colour. **The limit worth knowing**: a match spanning two tokens cannot be tinted, because the text is coloured token by token and `der(y)` is three spans — but the line is still found and stepped to, so it is reachable. No automated test covers the wiring between the two halves; that is B237. |
| B182 ✅ | **Line numbers for naming violations do not line up with the rendered code** | Code Review | ⭐⭐⭐ | M | A finding on a naming violation points at the wrong line in the viewer. The likely cause is that the finding's line is computed against the **stored** source while the viewer renders the **reformatted** source — the page reformats in order to colour it, so the two can differ by however many lines the formatter moved. Either the mapping has to go through the same representation the viewer shows, or the viewer has to stop reformatting. Establish which before fixing: the same mismatch would affect every rule whose line comes from a visitor, not only naming. **The viewer stopped reformatting (B215), so this closed as an identity rather than a fix**: a finding's line is counted against the class's own source, and that is now what is on screen. The guess in this row was right about the cause and right that it was not only naming — it was every rule. Measured before the change: only 3.7–5.8% of displayed lines were the file's line with the file's text on it. |
| B183 ✅ | **Findings do not scroll to their line, and arrive in no particular order** | Code Review | ⭐⭐⭐ | M | Two halves. Clicking a finding should scroll the viewer so the line it is about is **in view** — currently a finding on a parameter can select a class whose relevant line is off screen. And the list order is whatever the check produced; it should be stable and sorted alphabetically, so the same library reviewed twice presents the same list twice. **Both halves done with B215.** The order is by class, then by line within it, then by rule — which is how a reader works through a class, and is why it is not alphabetical by message. The scroll is a new `spellCheck.scrollLineIntoView` beside the word-scroll interop the spelling findings already used, and it only became possible because the viewer now shows the class rather than a copy 17–24% longer than it. A finding inside a collapsed nested class scrolls to that class's marker rather than nowhere, which is `SourceElision.ToDisplayLine` answering with the nearest honest line. |
| B186 ✅ | **The findings table runs off the bottom of the page** | Code Review | ⭐⭐⭐ | M | Part of the table is not reachable, particularly when each finding spans several lines. A splitter between the viewer and the findings list would let the user choose, but the row count shown has to vary with the space given to it as well, or resizing just moves which part is cut off. **Both halves done, and the row count does not vary — it is gone.** The table is virtualised and scrolled rather than paged, so every finding is reachable at any pane size and a real library's tens of thousands of them (26,557 for MSL) still cost only the rows on screen. The splitter is `MudExSplitPanel`, the control MainLayout already uses, turned the other way; the two fixed heights it replaces were `221px` for the table and `calc(100vh - 405px)` for the viewer, the second being the first plus 185 px for everything above it. `ResizablePanesJourney` drags it in a real browser and measures the pane, because that is the claim. |
| B187 ✅ | **The finding filter cannot filter by rule, and combines its terms with OR** | Code Review | ⭐⭐ | S | Two defects in one control: there is no way to narrow to a rule type, and entering more than one term widens the result instead of narrowing it. It should be **AND** — a partial model name *and* a keyword — which is what anyone typing two things into one box expects. **Both done, and the order mattered**: the class scope was smuggled through the search string (`_searchString + " " + NavState.ModelID`, stripped back out inside the filter), so swapping OR for AND first would have excluded every finding whose text did not also contain the class name — which is all of them. The scope and the rule are parameters now, and `CodeReview.Matches` is a pure function with fourteen tests over it. The rule list offers only the rules the current findings use, by catalogue title: offering all forty-odd makes the control something to search rather than something to pick from. |
| B178 ✅ | **The diff view uses its own colours instead of the syntax scheme** | Code Review | ⭐⭐ | S | Side-by-side diff markup does not follow the rendering colour scheme used when viewing a single file, so the same code is coloured two ways in two panes of the same page. The user's chosen preset should reach both. **Closed by the diff adopting `ModelicaTokenClassifier`** — it classifies each whole side once and indexes the result, rather than highlighting a line at a time, so the ~100 lines of keyword-list regex are gone. **The planning note's fix was not the one needed.** It said to share the *colour source* between two stylesheets, because the two highlighters "cannot be merged — a diff hunk is not parseable". Both halves turned out otherwise: the sides are whole documents, not hunks, so one classifier does serve both; and the preset already reached the diff, because the runtime stylesheet is global and every rule carries `!important`. What was actually wrong is that the regex could only produce four categories, so identifiers, types, names and functions were plain text in the diff and coloured in the viewer. Emitting all nine fixed it with no change to either stylesheet. |
| B185 ✅ | **A very large class can fail to render at all** | Code Review | ⭐⭐⭐ | M | An imported FMU — `Engines Examples I2 fmu` — was still not rendered after five minutes. Two candidate costs and they need separating before either is addressed: the syntax highlighting over a very large token stream, and the reformat the page performs in order to colour it. The likely answer is a size threshold above which the page shows plain source with no highlighting and no reformat, but measure which half costs the time first. Relates to B182, which is about the same reformat. **Measured, and it is neither half.** B215 removed the reformat; the highlighting costs 20–148 ms at every size tried. It is the **parse**, and not because of size — a run of comments inside an `equation` section makes it quadratic (**B235**), while 554 KB of annotated declarations parse in 367 ms. **The likely answer in this row was therefore wrong twice over**: there is no size to threshold on, and turning the highlighting off would have saved nothing. What ships instead is that the viewer stops *waiting*: above 64 KB the lexer paints the class at once and the tree's colouring replaces it when the parse lands, the difference being only whether an identifier is known to be a type or a call. The threshold is a judgement about when a stall would be noticed, not a prediction of one — said explicitly at the constant, because a number that looks like a prediction will be read as one. The cause is B235 and that is where the real fix is. |
| B213 ✅ | **A token classifier, so the viewer can colour the file instead of a rewrite of it** | Code Review | ⭐⭐⭐ | M | `ModelicaTokenClassifier` in `ModelicaParser`, beside `ModelicaRenderer`: walk the parse tree recording token index → category, then emit the **original** source verbatim with each token wrapped in the tag `CodeViewer` already consumes. Driven by character offsets, never by token text, so whatever the lexer skipped is copied through and the output is exact by construction. Three tiers — parse tree, lexer only, verbatim — so a class that does not parse is still coloured. Two property tests carry it: **round trip** (strip the tags, get the source back) over the fixture library, and **agreement** (same categories as `ModelicaRenderer` on already-formatted input). Measured at 99.98% / 99.77% agreement over 114,642 identifier tokens and byte-exact over 1.09M lines; see `analysis-viewer-fidelity.md` §4, §5, §9 and §14 S2. **Delivered** as `ModelicaParser/Visitors/ModelicaTokenClassifier.cs` with 31 tests. Round trip is asserted over a curated corpus always, and over **every `.mo` file in a real library** when `MLQT_FIDELITY_CORPUS` points at one — 8,367 files exact. Three corrections to the plan, all found by S0 and all in the walk rather than the emitter: the graphics level has **three** increment sites and the middle one is how a *named* argument's value gets one (that is what colours `DynamicSelect`); `der`/`initial`/`pure` are calls; and a dotted name is one tag to the renderer and one tag per IDENT here, so any comparison has to split it — but never inside a quoted identifier. **The tiering changed**: the lexer-only tier is chosen by the caller for a class too large to parse, not reached by failure, because the parser recovers rather than throwing. The one deliberate divergence from today's colours is B232. |
| B214 ✅ | **One range-elision mechanism, with a line map that inverts** | Code Review | ⭐⭐⭐ | M | Hiding annotations, hiding a package's class definitions, the MCP annotation strip and the package trimmer are four spellings of the same operation, and each is currently the renderer simply not visiting a subtree. In a verbatim view each becomes a dropped range of source lines, optionally replaced by a one-line marker. `SourceElision` — ordered, non-overlapping ranges plus `ToSourceLine`/`ToDisplayLine` — is that operation once, with invertibility as a property test. **Not deferrable**: every package opens with class definitions hidden, so this is how a package is displayed at all. `analysis-viewer-fidelity.md` §11b. **Delivered** as `ModelicaParser/Helpers/SourceElision.cs` (the mechanism) and `ModelicaParser/Visitors/ElisionFinder.cs` (the tree reading), with 30 tests; invertibility is a theory over nine range shapes and the package-collapsing gate is asserted directly. Overlapping ranges are refused rather than merged — there is no single answer for what the display shows, and a caller that produced them has a bug. **One decision that is not in the plan and is a real limit**: a construct is elided **as a unit or not at all**, so an annotation sharing its first or last line with code is left visible rather than leaving `Real x "d" annotation (Placement(` on screen. In MSL that hides the class-level graphics annotations, which are written on their own lines and are 41–44% of all lines, and keeps the inline `Placement` ones. Doing the mid-line case properly needs markup-aware splicing and is **B233**. |
| B215 ✅ | **The Code Review page shows the file, not the renderer's rewrite of it** | Code Review | ⭐⭐⭐ | M | Wire B213 and B214 into the page: the source rule (use the stored code while `SourceMatchesFile`, otherwise re-slice the file by `StartIndex`/`StopIndex` **with the decoder those offsets were recorded with**), the classifier in place of `ModelicaRenderer`, `ShowRawSource` deleted in favour of the lexer tier, and `PrependElementPrefix` inserting into verbatim text. **Closes B182, B183 and B185.** Measured: ~95% of displayed lines are currently not where the user's editor puts them, and the fidelity path is ~45% cheaper end to end. The page has no component tests today; they arrive with this. **Done, and it closed B182 and B183 with it — B185 is what is left** (the lexer-only tier is built and public but nothing chooses it yet, so the size threshold is still to come). `CodeReview.Show` is the whole of the new path: `ClassSource` for the text, `ModelicaTokenClassifier` for the colour, `SourceElision.Merge` for what is hidden, and the map kept beside the lines because one without the other cannot say which line of the class a displayed line is. **Three things went rather than being adapted**: `ShowRawSource` and the parser-error branch (the lexer tier colours what recovered, where the old path showed no colour at all), `LoadOriginalFileContents` orphaned with it, and `_modelicaCode`/`_lines`, which were written at three sites each and read nowhere. Ten tests on a page that had none; the one that matters asserts every displayed line equals the line the user wrote, over a deliberately loosely-formatted class the old path would have renormalised on its first declaration. |
| B216 ✅ | **`PackageCodeTrimmer` re-renders where it could excise** | Checking | ⭐⭐ | M | Trimming a package's inline standalone children runs them through `ModelicaRenderer`, so the checked text is a rewrite of the file and `SourceMatchesFile` goes false — which is why findings in those packages point at no line at all. Excising each child's `[StartIndex..StopIndex]` instead leaves a subsequence of the file, restores the line mapping, and improves the CLI and MCP reports as well as the viewer. **The riskiest change in the set**: the trimmer feeds the pipeline all three surfaces share, so the gate is the parity number (MSL = 34329) with no finding moving other than by the removed ranges. Two reassurances short of proof — the trimmer passes `FormattingOptions.None`, which reorders nothing, and no rule is sensitive to whitespace or line length. Decided by measurement before anything is built. **S1 decided it: in scope** — no finding is lost on either library, MSL gains 2 correct `OneOfEachSection` findings (excision leaves an empty section behind that re-rendering dropped, so it is baseline drift to declare), and the line map is exact over 30,599 lines. **Finish the file's mutation survivors with the rewrite.** Seven are left after B230 and each sits on a decision this item replaces or inherits: the selection rule at `:84` (both `&&` operands and the `"package"` literal), the three early returns at `:88`, `:96` and `:101` (nothing to exclude, the parse failed, a short class definition has no body), the parse-error guard at `:95`, and `excludeClassDefinitions: false` at `:106`. They have been in the WP8 audit report unclaimed since it ran; excision keeps the selection rule and the short-class case, and drops the rest with the render, so the honest end state is each one either killed by a test or gone with the code. **Done 2026-09-20.** The trimmer cuts the children's lines out instead of re-rendering the package, so what is left is the file's own text and `ModelNode.TrimElision` records which lines went - `ClassLocation.FileLine` takes the offset after putting them back. **Gate met on both libraries**: MSL 0 findings lost, exactly the 2 `OneOfEachSection` findings S1 predicted gained (and they are correct - `ReferenceAir.mo` really does have three public sections, which re-rendering merged), and **432 findings that used to be pinned to the class declaration now point at the line they are about**; Buildings 0 lost, 0 gained, 34 findings gained a line. Two things the item did not foresee. `SourceMatchesFile` was answering two questions - *do lines map* and *is this the whole class* - which the old trim made identical and this one separates, so `ClassSource` needed `StoredSourceIsWholeClass` or the diff view would have shown a trimmed package against itself. And the per-child file check had to be added: computing a child's range from a line number measured against a *different* file produced ranges that collided, and the whole trim was abandoned - B230's mistake one level down, caught by an existing test. **Survivors**: 3 of the 7 went with the render, 4 are killed by new tests, and the rest are recorded as judged-equivalent in the test file. One test written for them asserted nothing - a class named `package` cannot parse - and was removed rather than left. |
| B230 ✅ | **`PackageCodeTrimmer` re-renders packages that have nothing to trim** | Checking | ⭐⭐⭐ | S | A package whose standalone children are already in their own files has no inline child to remove — but the trimmer rendered it anyway, marked `SourceMatchesFile` false, and handed every later report a class whose lines were the renderer's rather than the file's. **377 of 688 trimmed packages in MSL (55%) and 1,172 of 1,235 in Buildings (95%) had nothing to remove**, and in Buildings 1,073 came out *longer* than the file. Fixed by one clause in the candidate filter: a child only counts if it shares the package's `ContainingFileId`. **Classes that lose their line mapping: MSL 688 → 311 (10.61% → 4.79%), Buildings 1,235 → 63 (16.44% → 0.84%)**, with the finding count unchanged on both (26,557 and 31,823) — a guard clause should move no finding, and it moves none. 174 MSL findings gain their true file line as a side effect; the remaining 654 are B216's. Sharing the file is the cheap half of the question — whether the child's `[StartIndex..StopIndex]` lies inside the package's is the exact one, and B216 asks it because it excises those ranges. `analysis-viewer-fidelity.md` §17.1. |
| B231 ✅ | **`ModelNode.StartIndex`/`StopIndex` are documented as something they are not** | Checking | ⭐⭐ | S | The field says "zero-based character offset ... in the underlying source file, read with `Encoding.Latin1` to match the parser" and that `StopIndex` is the last character, inclusive. Slicing a file that way reproduces **0 of 13,997 classes** across MSL and Buildings. They are offsets into the **line-ending-normalised** text `PreprocessCode` produces, and the slice that matches is `[Start..Stop+1]` — 99.49% / 98.38%, the residue being short class definitions, whose stored text carries a trailing `;` the slice does not. Every file in both libraries is CRLF, so a caller following the comment does not get a slightly wrong slice, it gets a different class. Correct the comment, and give the slice a helper so no caller has to know. `analysis-viewer-fidelity.md` §17.4. |
| B232 ✅ | **`ModelicaRenderer` colours array subscripts as function calls** | Code Review | ⭐ | S | `_isFunction` is a mutable field, not a scoped fact, and it shows in two ways. It **leaks into subscripts**: `VisitComponent_reference` visits the reference's `array_subscripts` while the flag is still set, so in `den2[i] := …` the subscript `i` is coloured as a call (377 tokens in MSL, 512 in Buildings). And a **nested call clears it** for the rest of the reference: in `m[integer(i),j] := …` the inner `integer(…)` resets it on the way out, so `j` loses the colouring the leak had given it (9 and 22). Found by the B213 agreement measurement, which is otherwise 99.998% — this is the entire residue. **The classifier should not reproduce either**, so closing this is mostly deciding that the visible change is wanted and saying so. **Done 2026-09-19.** `_isFunction` is saved and restored at each of the four sites that set it, and cleared around a reference's `array_subscripts`, which fixes both facets: a subscript is no longer a call, and a call nested in one no longer switches the colouring off for what follows. The classifier already declined to reproduce either, so the two now agree and `ASubscriptIsNotAFunctionCall_UnlikeTheRenderer` - which existed to record the divergence - is rewritten as `...AndTheRendererAgrees`. **Worth knowing before touching this again: the renderer's colouring is no longer what any user sees.** Every production call site passes `renderForCodeEditor: false` since B215; the tag-emitting mode survives only as the independent second implementation the classifier is measured against, which is the argument for fixing it rather than deleting it - a baseline with a known defect has to be explained every time it is used. Five tests, and the whole-library fidelity test re-run by hand (B234) since the renderer is that measurement's other side. |
| B235 | **A class with commented-out equations parses in quadratic time** | Parser | ⭐⭐⭐ | M | A run of comment lines inside an `equation` section makes `ModelicaParserHelper.ParseWithTokens` quadratic: **500 of them parse in 1.5 s, 1,000 in 4.4 s, 2,000 in 17 s, 4,000 in 69 s** — four times the work for twice the text, at only 323 KB. The same class with the comments removed parses in 86 ms, and 4,000 *annotated* declarations (554 KB, half as many lines again) parse in 367 ms, so it is neither size nor volume. **The input is legal Modelica and parses with zero errors**, so this is the parser's prediction rather than its error recovery — `equation_or_comment : (c_comment \| (equation ';'))` with `COMMENT` and `LINE_COMMENT` on the default channel is where to look, and `statement_or_comment` is the same shape. **This is not a viewer problem**: the check pipeline parses every class, so a library with commented-out equations costs the CLI and the MCP server the same minutes it costs the page. B185 only stopped the viewer *waiting* for it. Reproduce with `MLQT_LARGE_CLASS_COST=1` over `LargeClassCostTests`, which measures three shapes so the cause cannot be misattributed again — the first version of that measurement varied the annotations and the comments together and blamed the annotations. |
| B237 | **Nothing automated proves the code search is wired up** | Testing | ⭐ | S | B176's two halves are unit-tested — which lines match (`CodeReviewCodeSearchTests`) and what the markup looks like once tinted — but the wiring between them is not: the term travels from the field through `CodeReview` to `CodeViewer`, which re-renders only when it notices the parameter changed, and only a browser can say whether that happens. **A journey for it was written and then removed**, because it could not be made reliable in the shared host: searching needs a class selected, and the two ways to get one both fail there. Through the tree needs a *repository*, and registering one starts the analysis pipeline and leaves it registered — which failed the journey and then timed out `TabNavigationJourney` five minutes later. Clicking an injected finding avoids all that and **passes in isolation in 11 s**, but in a full run the code viewer never renders at all, for a reason in the shared host that was not found. The feature was verified by that isolated run and by the regenerated screenshot. Worth either a journey with a host of its own, or understanding why a class will not open when the other journeys have run first — the second is the more useful answer, because `ResizablePanesJourney` will hit it too the moment it needs a class. |
| B238 | **A rule switched on while its prerequisite is off is lost on the next save** | Settings | ⭐⭐⭐ | S | Found while doing B181, and it predates it. The four rules with a prerequisite were serialized through a bool facade whose getter answers the *effective* question - will this rule run - which is false while the prerequisite is off. So a rule the user had switched on was written as `false`, and because the facade's setter removes the map entry when given false, reading the file back deleted the entry `RuleSeverities` ahead of it had just restored. A repository that turned `OneOfEachSection` off lost its ordering rules at the next save, silently, while the dialog went on showing them ticked - it binds to `IsRuleSwitchedOn` precisely so an inert rule still reads as configured, which is what hid it. The bool reconciliation was built to be order-independent and is, for `true`; the asymmetry is that disabling removes unconditionally, right for a caller saying "off" and wrong for a redundant `false` that only means "not currently in effect". **Fixed 2026-09-19**: the legacy JSON name carries the configured value and the effective facade is out of the JSON surface, so old settings files still load and no C# caller changes meaning. Six tests, driven off `RuleCatalog` so a fifth rule with a prerequisite is covered the day it is added. |
| B239 ✅ | **Nothing enforced the rule that every Modelica file read and write goes through `ModelicaFileEncoding`** | Testing | ⭐⭐ | S | Raised while reviewing B236: the funnel held because people remembered, not because anything failed if they did not. That is the shape this backlog keeps naming - a promise in CLAUDE.md with no test behind it - and B236 is what it looks like when it goes wrong, two write paths disagreeing for the whole life of the project. **Done 2026-09-20**: `ModelicaFileAccessPolicyTests` scans production source for raw file-text APIs and requires each to be in a ledger with a reason. It cannot tell what a `path` variable holds, so it does not guess - it puts the judgement in front of a human once and keeps it visible, the same trade `CodeBehindPolicyTests` makes. Counts are part of each entry, so a second call in an already-listed file is not covered by the first one's reason. Verified by mutation: pointing the incremental formatter at `File.WriteAllTextAsync` fails it. It found two sites a manual grep had missed and one real gap (B240), and the picker's Modelica entry point now uses the funnel. |
| B240 | **A conflicted file's `.mine`/`.rN` sidecars are read outside the encoding funnel** | Revision control | ⭐ | S | Found by B239's scan. `SvnRevisionControlSystem.GetConflictVersions` reads the two sidecar files SVN leaves beside a conflicted file with `File.ReadAllText`, which is a UTF-8 decode - so for a Windows-1252 library the conflict diff shows replacement characters where the accented characters and curly quotes are. **Read-only and display-only**: it feeds `ConflictDiffDialog` and nothing writes the result back, so the cost is a diff that looks wrong rather than a file that becomes wrong. **The fix is not simply to call the funnel.** `RevisionControl` has no project references at all - it is deliberately Modelica-agnostic - so reaching `ModelicaFileEncoding` means either a new dependency, which gives up that property, or passing a decoder in from the caller. Worth deciding rather than doing. |
| B234 | **The strongest test MLQT has does not run unless someone remembers a variable** | Testing | ⭐⭐ | S | `ModelicaTokenClassifierTests.RoundTripsOverAWholeLibrary` asserts the whole fidelity claim — strip the tags and the output is the source, over **8,367 files and 1.1M lines** of real Modelica. It is opt-in through `MLQT_FIDELITY_CORPUS` because a library that size is not in the repository, which means **neither CI nor `run-mutation.ps1` ever executes it**, and that is visible in the mutation report: the emit loop's offset arithmetic is where the surviving mutants are, because the only test that would kill them does not run. The committed corpus was widened (no trailing semicolon, trailing blank lines, CR-only endings) but that is a mitigation, not a fix. Three ways out and they are not equivalent: commit a corpus large enough to exercise the emit loop (how large is the question — the shapes that matter are multi-line tokens, skipped characters and text `PreprocessCode` has to tidy, not volume); make the CI job fetch a library; or accept it as a manual gate and put it in the release checklist so it is run deliberately rather than remembered. **Run it by hand before any release that touches the classifier** — the command is in CLAUDE.md. |
| B233 | **Hide-annotations leaves the ones written on a line with code** | Code Review | ⭐ | M | `ElisionFinder` removes a construct **as a unit or not at all**, so an annotation whose first or last line carries real code stays visible — dropping whole lines from it would leave the user looking at `Real x "d" annotation (Placement(`, which is worse than showing it. That is the right default and it hides what matters (41–44% of lines sit wholly inside an annotation, and the class-level graphics ones are written on their own lines), but the inline `Placement` annotations on declarations are exactly the noise a user asked to hide and they remain. Doing it properly means splicing **markup**, not source — the elision is applied to highlighted lines, so cutting a line at a character offset can cut inside a tag. The shape is probably for the classifier to expose where each token's markup begins so a line can be rebuilt from whole tags. Worth measuring what fraction of annotations this actually leaves before deciding it is worth that. |
| B217 ✅ | **The class diff compares stored text against the file** | Code Review | ⭐⭐ | S | `LoadModelDiffAsync` feeds the diff `Definition.ModelicaCode` as the working copy and a slice of the **file** at HEAD. For a package whose inline children have been trimmed, or a class re-rendered by the formatter since it was read, those are not comparable documents — the trimmed children should be appearing as deletions. **Predicted, not observed**: confirm it against a package with standalone children and an uncommitted change before writing anything. B215's source rule fixes it as a side effect. **Confirmed, and the confirmation is the test**: a package the trimmer has touched holds text with its inline children removed, so every one of them showed as a deletion nobody made. `CodeReview.WorkingCopyText` now takes the working-copy side from `ClassSource` like the viewer does, so both sides of the diff come from the file. The control matters as much as the fix — an ordinary class, which is the large majority, still answers from its stored text without going near the filesystem. |

### Library browser and navigation

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B188 | **Repositories cannot be reordered within a project** | Library browser | ⭐⭐ | S | They appear in the order they were added, which is rarely the order of interest. The user wants the most relevant at the top. Needs a persisted order in the project settings and a way to change it. |
| B189 ✅ | **Opening a model from a finding does not reveal it in the tree** | Library browser | ⭐⭐ | S | Clicking a finding opens the model in the viewer but leaves the browser wherever it was, so there is no context for what was just opened. Expand to and select the model, the way a "reveal in tree" action would. **Done**, and it reuses the tree's existing restore-expansion walk, which already knows to fetch children for a node marked expanded — a node expanded without them renders open and empty. Two things worth knowing: the chain of packages is walked through `ParentModelName` rather than by splitting the dotted name, because containment is what the tree nests by and a quoted identifier carries dots of its own (`ModelicaReference` has one); and a browser whose tree does not start at that chain's root returns without touching anything, or every repository's tree would expand for every class. A selection the tree itself made is not revealed — the user has just clicked it, and re-expanding underneath them is not help. |
| B191 | **Nothing in the tree marks a model as modified** | Library browser | ⭐⭐⭐ | M | Add a marker for models with uncommitted changes, and **distinguish the kind of change**: one marker for edits that affect simulation, another for purely graphical or documentation edits. Then a filter view showing only modified models, filterable by that distinction. The classification is the substantial part — it needs a comparison of the parsed old and new class, not a text diff. |
| B197 ✅ | **No way to peek at or navigate into a used class** | Library browser / Code Review | ⭐⭐ | M | Reviewing documentation, what looks like a variable name is highlighted and checking what it should be called means finding the base class by hand. Wanted: a quick peek, or navigation into the base class with a way back — ideally back through the last few classes visited, like an editor's navigation stack. **Navigation in and the way back, not the peek.** A menu on the Code Review toolbar lists the classes this one uses and opens one; back and forward arrows sit beside the class name, because every tab moves the selection and the history is one history — so it lives on `AppState` with the selection it tracks, not on a page. Going somewhere new after going back discards what was ahead, re-selecting the class already shown is not a visit (a reload re-raises the selection and would otherwise fill the history with one class), and fifty is as far back as it remembers. **What is not done is the peek**: hovering an identifier in the documentation and seeing its class without leaving. That needs a token to be resolved to a class, which the classifier's `TYPE` and `NAME` tags make possible but do not do. **And the menu is empty until dependency analysis has run**, because the edges are what it reads — it says so rather than looking like a class that uses nothing. |
| B173 ✅ | **The resource tree has no horizontal scrollbar and cannot be resized** | External resources | ⭐⭐ | S | A long path in the External Resources tree is simply cut off: the panel neither scrolls sideways nor resizes. Either would do; both is better, and a splitter here would be the same control B186 wants on the Code Review page. **Both done**: the fixed 4/8 grid became the same `MudExSplitPanel`, and the pane scrolls on both axes (`overflow: auto`, not `overflow-y`) so a path too long for whatever width the user chose can still be read. |
| B199 | **The dependency plot is unusable above a few hundred nodes** | Dependencies | ⭐⭐ | S | Over 1,000 nodes the Cytoscape graph is very slow to generate and the nodes are too small to read, so it costs a lot and shows nothing. Above a threshold, skip the plot and show the list alone — with a way to draw it anyway if the user insists. |

### Projects, repositories and startup

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B198 | **A newly added repository did not load its library until MLQT was restarted, and has not reproduced** | Repositories | ⭐⭐ | M | Reported once, against `ModelicaEditorTestsGit`; restarting the tool loaded it. **Not reproduced after several attempts** on a build carrying the fixes below. Two candidates were investigated and both are ruled out. The *layout* — a single library with `package.mo` at the top level — is disproven by `AddRepositoryLoadsItsLibraryTests`: such a library is discovered, loaded, recorded on the repository, announced to the tree, and visible to the `Libraries.Where(RepositoryId == id)` query MainLayout gates its analysis on, with a subdirectory layout as the control. **B204** — the discovered library being labelled with the folder name rather than its own — would have looked like this, but the library in that repository has the same name as its folder, so it was never visible there. **Left open at low confidence**, like B166. It is worth noting the report came before B192 was fixed, and that fix removed a path on which a *different* project's repositories were loaded at startup and left loaded — enough to confuse what belonged to what. If it recurs, B206 now makes it report itself: the two silent outcomes (nothing found, nothing loadable) each say so, so the next sighting can distinguish "no library discovered" from "discovered but not loaded" from "loaded but not shown", which the original report could not. |
| B190 | **The UI, and sometimes the whole desktop, freezes during loading** | Performance | ⭐⭐⭐ | M | Reported during the loading step: MLQT's window is unresponsive, and at some points other windows are slow to redraw too — moving the MLQT window is sluggish and everything can stall for a few seconds. **Confirm it still happens before investigating**; it predates several fixes since, and the log records phase durations that would say where the time goes. Whatever is blocking is on a thread it should not be on. |

### Analysis correctness

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B184 | **`--changed-from` re-checks everything it loaded** | CLI / performance | ⭐⭐⭐ | M | A changed-file check still runs every rule over every model, then filters. It should apply the rules only to the models in the modified files and compare those against the **baseline records for those models**. Everything still has to be *loaded* — base classes and reference resolution need it — but it need not be re-checked. The one analysis that genuinely has to run over everything is reference validation, and only when a model was deleted or renamed. This is the largest available win on check time in CI. |


### Rules and formatting

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B175 ✅ | **Exclude-from-formatting writes a name into the settings file rather than an annotation** | Formatting | ⭐⭐ | S | The button records the class in `FormattingExcludedModels`, which is name-based and so does not survive a rename. `__MLQT(format=false)` is the rename-safe mechanism the documentation already recommends, both mechanisms are already honoured everywhere (B39, B65), and the writer already exists for suppression. The button should write the annotation. **Done 2026-09-19.** The button now writes `__MLQT(format=false)` into the class, through `MlqtSuppressionWriter`, and reads its own state through `FormattingExclusion.Excludes` rather than the name list alone - it was showing a class carrying the annotation as *not* excluded, so the button offered to exclude something that already was. **The row said the writer already existed, and half of it did.** Adding needed a bare directive rather than an entry appended to a quoted list, and *removing* one had never been needed by anything: the toggle has two directions, so `TryRemoveFormattingOptOutFromFile` is new. Removal widens as each container empties - the argument while `__MLQT` still holds something else, the whole `__MLQT(...)` when it does not, and the whole annotation and its `;` when that was all it held - because the alternative is leaving `annotation(__MLQT());` in a user's file. Re-including also clears the name list, or the button could not undo what earlier versions of itself had done. Reverting a modified file now happens *before* the annotation is written rather than after, which would have discarded it. 21 tests, including add/remove round trips that must return the original file byte for byte, and one across the two assemblies asserting that what the writer writes is what `FormattingExclusion` reads. |
| B177 ✅ | **No warning when a package is stored as one file instead of a directory** | Rules | ⭐⭐ | M | A package held entirely in a single `.mo` file rather than as a directory of standalone classes is a structural choice worth flagging in some repositories and not others, so it needs a rule id, a severity, a switch in the settings dialog and a row in the Findings list — not a hard-coded warning. Belongs with `PackageOrderAnalyzer`'s family. **Done 2026-09-19.** `MLQT.Structure.SingleFilePackage`, a graph analysis beside `PackageOrderAnalyzer`. The judgement is **could this be split**, not how big the file is: a child that cannot be stored standalone - `replaceable`, `redeclare`, `inner`, `outer` - has to live in its parent's file, so a package made only of those is correctly one file and is not reported, and neither is a package someone has already begun splitting, because "entirely in one file" is the claim. Off by default, like every rule, which is the point of it being a rule: single-file storage is a repository's choice. Nine tests, and the Structure category is catalog-driven so the settings dialog picked it up with no layout row. |
| B181 ✅ | **"Components before classes" can only be enforced by the formatter, never reported** | Rules | ⭐⭐ | S | The composition ordering rule is formatting-only: it is applied when the renderer rewrites a class and there is no way to be *told* about it. If the rule is switched on it should raise a finding like every other ordering rule. Note this row is the one in `settings-reference.md` with no rule id to bind its label to (B103), so giving it an id closes that gap as well. **Done 2026-09-19.** `MLQT.Style.ComponentsBeforeClasses`, with the visitor, the catalogue row, the layout row, the settings-reference id (which closes B103's gap - that row was the only one with no id to bind its label to) and the MCP toggle. Two things were not obvious from the row. **The section is the unit, not the class**: the renderer groups components before classes within each `public`/`protected` section and never moves an element across the boundary, so a class definition in one section followed by a component in the next is correctly ordered and comparing over the whole class would report the arrangement formatting produces. And its prerequisite is `ImportStatementsFirst`, not `OneOfEachSection` like its neighbours, because the renderer reads the option only inside the branch imports-first selects; the chain resolves transitively, so naming the nearer one states the real dependency. The settings switch now binds through `IsRuleSwitchedOn` like the four beside it, which it could not while it was a plain bool. |
| B236 ✅ | **Formatting strips the trailing blank line from every file, so a reformat rewrites the whole library** | Rules / formatting | ⭐⭐⭐ | S | Reported 2026-09-19 from ordinary use. **Format All Files** now writes every file without a final newline where it used to end with one, so a user who reformats sees *every file in the library* as modified against a revision formatted by an earlier build — a diff of thousands of files in which nothing has actually changed, and which buries any real change in it. **A regression, and it predates 2026.4.0**: the reported comparison is against a library formatted by an earlier version, and the release reproduces it. Two things worth knowing before hunting it. The write path ends `var code = string.Join("\n", visitor.Code)` (`ModelicaPackageSaver.cs:191` and `:357`) handed to `ModelicaFileEncoding.WriteAllText`, which writes exactly what it is given — so a trailing newline can only come from `visitor.Code` ending in an empty line, and the question is what used to put it there. And `ModelicaParserHelper.PreprocessCode` does `NormalizeLineEndings(...).TrimEnd()` before the parse, which is one way a trailing blank line stops existing early. **Bisect rather than reason**: this is a one-character output difference over a path several rewrites have crossed, and `git log` on those two files against a formatted fixture will find it faster than reading will. Whatever the cause, the fix wants a test that writes a file and asserts the last character, because nothing currently looks. **Fixed 2026-09-19, and the diagnosis in this row was wrong: it is not a regression.** `git log -S` settles it - the incremental formatter has appended a trailing newline since the initial public commit and the full library save has joined the rendered lines without one since the same commit. The two have never agreed, so which path last touched a file decided how it ended, and the user's own guess on the phase note - that the earlier reformat went a different route - was right. `package.order` was already terminated, because `WriteAllLines` mirrors `File.WriteAllLines`, so one save left the two kinds of file in a package ending differently. The rule now lives in `ModelicaFileEncoding.EnsureFinalNewline`, applied by every write and public so a caller comparing what is on disk against what it would write compares like with like - MCP's `format_class` needed exactly that, or it would have reported every formatted file as changed and rewritten it on each call. **Measured over MSL**: 5,753 files, none left without a final newline, and of the 2,480 files both paths rewrote the two now produce identical bytes for 2,471. The 9 that differ are all `package.mo` and are not a formatting disagreement - the full save extracts inline standalone children into their own files and the incremental path does not, which is the documented one-file-per-class restructure. Six tests, three of which state the route-independence property rather than the newline. |
| B195 ✅ | **`package.order` completeness, with an option to align with Dymola** | Rules | ⭐⭐ | M | Dymola emits, of its own accord on load: *"Warning: The package.order is incomplete, since the class Bad in file/directory .../Sub/Bad.mo is missing."* That is exactly the "a warning may be given" the specification anticipates, and it confirms the shape recommended for MLQT's own check. **It fires only for the `package.order` shape** — Dymola never descends into non-package folders, so it gives no signal at all for the stray-file case, and that half of MLQT's check has no upstream equivalent and earns its keep on its own. Worth an explicit "match Dymola" setting so a repository can choose to report only what Dymola would. **Done 2026-09-19, and the row was wrong about the interesting half.** `PackageOrderMatchesDymola` narrows `MLQT.Structure.PackageOrder` to what Dymola's loader would warn about. It is a modifier on one rule rather than a rule of its own - it decides which findings are reported, not whether anything is checked - so it is a switch under the analysis rules rather than a severity picker among them. **The 'stray-file case' this row rests on does not exist**: MLQT does not descend into a directory without a `package.mo` either (`LibraryDataService`, same reason Dymola does not), so there is no half of the check with no upstream equivalent on that side. Found by running the CLI over a fixture with a file in a plain folder and watching it not load. What the setting really drops is two things: **stale entries**, which Dymola says nothing about, and **a class in a file whose name does not match it** - MLQT reads every `.mo` in a package directory and takes each class's own name, so it loads `model Odd` out of `Widget.mo` and reports it unlisted, while Dymola resolves by file name and cannot load it from there at all. That second one is worth keeping in the default answer: a class the tool of record cannot find is a worse problem than the one this rule usually reports. Eight tests plus an end-to-end CLI check of both answers. |
| B174 | **Loading and style checking are getting slow** | Performance | ⭐⭐⭐ | L | Reported from ordinary use. B128 established the series and the per-phase breakdown — `CheckTimings` is in place and `mlqt check --timings` prints it — and concluded the growth to ~300 s was expected rather than a regression, with no next obvious win at the time. **Start from the log**, which records timestamped phase durations going back weeks, and from `--timings` on the current build; do not theorise first. B184 and B147 are the two named candidates. |

### External tools

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B170 | **An OpenModelica check reports nothing at all** | External tools | ⭐⭐⭐ | S | Checking a class with OpenModelica produces no visible feedback: it opens no window, and there is no dialog reporting success or failure. The Dymola equivalent is only known to have run because Dymola's own window appears. Both need a result dialog of MLQT's own, reporting what the tool said — which also removes the dependence on a vendor window being visible. |
| B171 | **Dymola checking fails on the second attempt if its window was closed** | External tools | ⭐⭐⭐ | M | The first check works; closing the Dymola window and checking again does not. The interface presumably keeps a connection or process handle that is no longer valid and does not detect it. It should notice a dead session and start a new one — and, with B170 in place, say so if it cannot. |

### MCP server and McpTester

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B179 | **MCP loads an encrypted library but returns almost nothing about it** | MCP | ⭐⭐⭐ | M | An agent can load an encrypted library, but the tools do not return the public interface or the documentation — both of which **are** recovered from the vendor's help HTML and are sitting on the synthesized stub: description, base classes, parameters, connectors, inputs and outputs. `get_class_info` and its neighbours should return them for a stub, clearly marked as recovered from documentation rather than read from source, and still declaring the class not editable (B85). |
| B196 | **The diagram tools give an agent no way to see what it drew** | MCP | ⭐⭐ | M | The diagram tools return a description, so the agent is laying out a picture it cannot look at. Rendering the SVG to **PNG** and returning it would let the agent judge its own layout — and the SVG is already produced, so the question is which conversion to take on (and what it costs in dependencies). Alongside that, the guidance the tools give needs to say how connector positions should be chosen to match MSL and the usual conventions, and it is worth asking what else would make an automatically generated diagram look right. |
| B218 | **`get_class_source` hands an agent a rewrite when it strips annotations** | MCP | ⭐⭐ | S | With `includeAnnotations: false` the tool re-renders the class through `ModelicaRenderer`, so the text an agent reads is not the file and its line numbers do not match the findings the same server reports. B214's elision strips the annotations without touching anything else, and then they do. Small, independent of the viewer work, and it can be taken in any phase after B214. |

### Revision control

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B193 | **Switch Branch cannot reach a Git tag** | Git | ⭐⭐ | M | Switching to a tagged version of `ExternData` is not offered, although TortoiseGit does it on the same working copy. The branch selector presumably enumerates branches only; checking out a tag produces a detached HEAD, which the surrounding UI also has to be able to describe rather than showing an empty branch name. |
| B202 | **The VCS History file diff compares against the working copy** | Git / SVN | ⭐⭐ | S | Clicking a file in a commit's changed-files popover opens a diff of that revision against the **working copy**. What a user reviewing history wants is what that commit *changed* — the selected commit against its predecessor. B155 corrected the documentation of the current behaviour after finding it reported confusing numbers; this changes the behaviour to the one the dialog is for. |

### Test fidelity — from the whole-solution mutation audit

Opened 2026-09-19 from `build/run-mutation.ps1 -All` over all seven measured assemblies (5h 40m;
23,634 mutants run, 5,990 survived, 4,159 in `MLQT.Shared` code no test executes at all — which is
the coverage ratchet's existing, reasoned debt and not a finding here). What *is* a finding is
below, and it has one shape: **the invariants this repository documents most carefully are the ones
with no test behind them.** Each line cited carries a comment explaining why the guard matters, is
executed by the suite — so the coverage gate is satisfied — and can be deleted or inverted without a
single test objecting. Regenerate the survivor list with `-Summarise`; it is git-ignored.

| # | Item | Area | Value | Effort | What is needed |
|---|------|------|-------|--------|----------------|
| B219 ✅ | **Nothing notices if an external-stub write/report guard is removed** | Checking | ⭐⭐⭐ | S | `LibraryCheckSession.cs:44` and `FormattingPipeline.cs:144-145` (`SourceType != EncryptedDirectory`, `!ReferenceOnlyScope.IsReference`) could each be inverted with nothing objecting — every test asserted the *included* outcome, so the thing that should have been excluded was never in the list. `ExternalStubWriteGuardTests` now asserts the excluded outcome with its positive control in the same test, and the three `Where` clauses moved to `FormattableLibraries` so the write guard is answerable without running a hundred-line save. Two corrections to the original row: **`GraphAnalysisContext.cs:47` does not belong here** — its survivor forces the filtering branch, which filters a list with nothing to filter, and the guard itself is tested; and `LibraryCheckSession.cs:44` **still survives**, because `StyleCheckRunner` filters stubs again downstream, so removing the first of two filters changes no observable result. |
| B220 ✅ | **The formatting-exclusion decisions are covered but unchecked** | Rules / formatting | ⭐⭐ | S | One of the three was real. `StyleCheckingSettings.cs:310` caches the compiled exclusion patterns, and the mutation making it ignore an edit survived `EditingTheListTakesEffect` — because that test goes empty → one → empty and `IsLibraryExcluded` returns on an empty list before the cache is consulted. Replacing one entry with another is what reaches it, and is what a user does when correcting a name in Settings; three tests now cover replace, grow and shrink, and the mutant is dead. **The other two were equivalent mutants**: `FormattingExclusion.cs:57` and `CoverageDimensions.cs:147` are short-circuits in front of a slower answer that agrees, so forcing the slow path changes nothing. Recorded because "a survivor is a missing test" is the wrong reading, and this row asserted it. |
| B221 ✅ | **Nothing notices if a parse diagnostic becomes suppressible or baselineable** | MCP / CLI | ⭐⭐ | S | "Always reported, never configurable, never baselined" was enforced in four places and tested in none. `SuppressionTools.cs:64` refused a diagnostic and no test ever passed it one, so both the `\|\|` and the `"MLQT."` prefix could be emptied — a theory over five ids, bare and prefixed, now asserts the refusal *and* that the source is untouched. `StyleTools.cs:286` passes `honorSuppressions: true`; flipping it made check_library report findings the user had waived, which is worse than noise because an agent will go and "fix" them. `MetricsRecorder.cs:92` excluded diagnostics from the recorded debt count, and the `&&`→`\|\|` mutant survived because no test had a diagnostic in the set. All four mutants confirmed dead. `Baseline.cs:191/267` is left open: its survivors change whether an empty `ExcludedLibraries` is written as `[]` or omitted, which is a serialisation shape rather than a guard. |
| B222 ✅ | **`ImpactAnalysisService` computed an SVG layout nothing read** | Dependencies | ⭐⭐ | S | Deleted: `CalculateForceDirectedPositions` and `CalculateEdgeEndpoints` (about 165 lines of circular placement, deterministic jitter, a 50-iteration force relaxation and clamping), `NetworkNode.X`/`.Y`, `NetworkEdge.X1`–`Y2`, `ImpactAnalysisResult.SvgWidth`/`.SvgHeight` and the two `svgWidth`/`svgHeight` parameters — 243 lines net. **Nothing in the product referenced any of them**: the build broke only in the test project, which is the proof. The three tests that kept it alive are gone too; each looped over a collection asserting a bound the code could not violate (`node.X > 0` against a clamp of 40, `SvgWidth >= 700` against `Math.Max(700, ...)`) and would have passed on an empty result. Replaced by two that can fail — every edge end is a node in the same result, and the node set is exactly the selected plus impacted models, by count as well as identity. |
| B223 ✅ | **The read-only-settings guard on a repository is unchecked** | Repositories | ⭐⭐ | S | `RepositoryService.cs:634` sets `IsSettingsReadOnly` when `.mlqt/settings.json` cannot be written, and `:317` is the only thing that reads it — the warning telling the user their per-repository settings are being ignored. Both could be removed silently; both mutants are now dead. Tested by making the write fail portably (a *file* where the `.mlqt` directory must go, so `Directory.CreateDirectory` throws the `IOException` the guard catches) with an ordinary repository as the positive control. `:241` stays as it is: its survivor changes only whether a log line is emitted. |
| B224 ✅ | **The test suite committed to the MLQT repository itself** | Test harness | ⭐⭐⭐ | S | Running `run-mutation.ps1` over `MLQT.Services` made `RepositoryServiceTests` create two branches in the MLQT checkout, commit to one as `MLQT User <user@mlqt.local>`, detach HEAD and discard every uncommitted change in the working tree — twice, on 2026-09-18 and again on 2026-09-19, the first time silently moving two days of commits onto a branch nobody created deliberately. The tests build a real git repository under the temp directory and are correct; what was missing was any statement that the service still had hold of it. A string in `RepositoryService` mutated to `""`, git fell back to the process working directory, and that is the checkout. **This generalises**: any mutated code doing file or VCS work can act on the repository the run started in. Fixed by `SandboxedId`, which proves the repository is under `Path.GetTempPath()` before returning its id, on all 61 call sites, with `SandboxedId_RefusesARepositoryOutsideTheTempDirectory` as its positive control. The SVN tests against a fixed path outside the sandbox are the deliberate exception and stay unguarded — recorded at the helper. |
| B225 ✅ | **`--force-with-lease` was promised in two interfaces and pinned by nothing** | Git | ⭐⭐⭐ | S | `GitRevisionControlSystem.cs:1597` builds `push --force-with-lease`, `IRevisionControlSystem.ForcePush` and `IRepositoryService.ForcePushAsync` both document the lease, and the string appeared in **no test in the solution** — the mutation audit could replace it wholesale unnoticed. Dropping to a plain `--force` passes every other test here and silently deletes a colleague’s pushed commits the first time two people share a branch. Now tested as behaviour rather than as a flag: with a bare remote and a second clone that pushes first, the force push must fail **and their commit must still be on the remote** — the second half is what a plain `--force` breaks. A positive control covers the un-diverged case, and swapping the flag was confirmed to fail the test. |
| B226 ✅ | **The formatter could overwrite a file it had failed to parse** | Formatting | ⭐⭐⭐ | S | `IncrementalFormatter.cs:143` returns rather than writing when the render reported parse errors — "reformatting invalid Modelica produces unreliable output, and this overwrites the file in place". Deleting that `return` broke no test: nothing had ever handed `FormatAndWriteAsync` a file it could not parse, on the path that runs **at startup and after every VCS operation**. `IncrementalFormatterWriteTests` now covers it, with the formatted case as its positive control and a third test holding the scope of the guard — one unparseable file must not stop the others being formatted, or a single syntax error silently switches formatting off across the repository. |
| B227 | **`ModelicaRenderer`: 361 surviving mutants, and it rewrites every line of a user’s library** | Formatting | ⭐⭐ | L | 2,393 mutants run, 85% killed — a good rate over much the largest body of decisions in the solution, and still the biggest absolute count anywhere in the audit. Each survivor is one layout decision the suite executes and does not depend on, in the code that rewrites a library in place. Not a defect list and not a score to chase: the work is to read them in groups (indentation state, section ordering, annotation placement, line breaking) and decide per group whether the behaviour is specified anywhere. Expect a meaningful share to be equivalent — two spellings of the same output. **Do not start this without a way to sample**; 361 read end to end is how a pass like this turns into tests that assert the renderer’s current output rather than its contract. |
| B228 | **`ModelicaFileEncoding` loses a byte-order mark, and can ignore an explicit encoding** | Parser | ⭐⭐ | S | Four survivors that are not equivalent. `:132` inverts `!File.Exists`, so an existing file is written as `Default` instead of in the encoding it was read in — harmless for the bytes, because the fast path is only taken for ASCII text, but a **UTF-8 BOM is dropped**, which is a change to a file MLQT was asked to preserve. `:112`, `:168` and `:175` are `encoding ?? …` null-coalescing mutations: an explicit `encoding` argument can be discarded with no test objecting. The `IsAscii` survivors on `:154`/`:158` are **not** part of this — they push onto the slower path that agrees, exactly as the method’s own comment says. |
| B229 | **`DymolaCheckingService` and `OpenModelicaCheckingService` are the least-guarded code in the solution** | External tools | ⭐⭐ | M | 11% of covered mutants killed in each — 130 and 122 run, 116 and 108 surviving. They are the only two services whose suites **no CI job runs** (`DymolaInterface.Tests` and `OpenModelicaInterface.Tests` need a live tool), so nothing outside one developer’s machine exercises them at all, and the mutation audit measured what that is worth. These parse a tool’s output into findings the user then acts on, so a survivor here is a misreported check result rather than a cosmetic one. Needs a live Dymola or `omc`; `build/run-all-tests.ps1` is the only thing that runs them today. Note B116 is already open against `omc` 1.26 on the same suite. |
